# 需求差距分析报告
> 基于《260408 智能体舱优化需求文档（补丁）V2.0》对照现有代码进行分析
> 分析日期：2026-04-09

---

## P0 — 本轮必须完成（核心功能缺失）

### 1. 术语全量替换
- **现状**：全站所有页面写的是"企业码"、"企业管理"、"企业分配"
- **需求**：全部改为"组织码"、"组织管理"、"组织分配"
- **范围**：前台、后台、所有弹窗文案

---

### 2. 组织码规则升级
- **现状**：没有格式限制，数字字母都能填
- **需求**：只允许 4~8 位英文字母，大小写不敏感，展示统一大写
- **涉及**：前端输入校验 + 后端存储校验

---

### ✅ 3. 用户类型与角色体系重构（已完成 2026-04-09）
- users 表新增 `user_type`（personal / organization）和 `role`（super_admin / system_admin / org_admin / user）字段
- 用户管理页新增用户类型、系统角色两列，支持按类型/角色筛选
- 管理员可点击角色标签直接修改用户角色

---

### ✅ 4. 固定三级组织结构（已完成 2026-04-09）
- 新建 `departments`（部门）、`teams`（小组）表，users 表新增 `dept_id`、`team_id` 字段
- 租户管理页支持展开组织结构，可内联增删改部门和小组
- 用户管理页新增"分配部门"按钮，可将组织用户分配至指定部门/小组

---

### 5. 注册入口与登录方式升级
- **现状**：没有注册页，用户首次用企业码+初始密码自动创建账号；密码最少 6 位
- **需求**：
  - 新增独立注册页，区分个人用户 / 组织用户
  - 注册需填用户名、真实姓名、手机号、密码（至少 8 位）
  - 登录支持手机号或用户名两种方式
- **缺失字段**：users 表缺少 `username`（用户名）、`real_name`（真实姓名）字段

---

### 6. 权限与可见性模型升级
- **现状**：智能体和工作流只能按"组织"整体分配（tenant_agents 简单关联表）
- **需求**：支持按组织 / 部门 / 小组 / 分组 / 具体用户 / 用户类型控制可见范围，双向操作入口，底层统一一张权限关系表
- **缺失**：需新建统一权限关系表，替换现有 `tenant_agents`

---

### 7. 工作流步骤类型扩展
- **现状**：只有"智能体执行"和"人工执行"两种类型
- **需求**：扩展为四种：智能体执行 / 人工执行 / 人工审核 / 其他（外部 AI 工具）
- **涉及**：数据库 `exec_type` 字段枚举值扩展 + 前台步骤标签视觉区分

---

### 8. 工作流可见权限操作优化
- **现状**：只能手动输入组织码字符串（如 `DEMO,ACME`），体验差
- **需求**：改为下拉选择组织，支持搜索，不再靠手输组织码

---

### 9. 首页联系二维码后台配置
- **现状**：首页有二维码展示位，但后台没有上传入口
- **需求**：在品牌设置中增加二维码上传、替换、删除功能
- **缺失**：`system_settings` 表缺少二维码相关配置项，品牌设置页缺少上传区域

---

### 10. 列表页筛选功能补齐
- **现状**：大多数列表页只有搜索框，没有多条件筛选
- **需求**：
  - 用户管理：按组织、用户类型、角色、状态、部门、分组筛选
  - 工作流管理：按分类、可见范围、组织、状态筛选
  - 智能体管理：按类型、分类、可见范围、分配组织、状态筛选

---

## P1 — 本轮建议完成

### 11. 批量操作
- **现状**：全部是逐条操作，没有批量能力
- **需求**：
  - 智能体分类分配弹窗：一键全选 / 取消
  - 智能体组织分配弹窗：一键全选 / 全部取消
  - 用户列表：批量启用 / 禁用

---

### 12. 工作流后台分类管理
- **现状**：工作流只有一个自由文本"分类标签"字段，无结构化分类
- **需求**：建立独立的工作流分类管理模块，可按部门、业务线、场景等分类

---

### 13. 组织删除逻辑
- **现状**：只能禁用组织，不能删除
- **需求**：
  - 有关联数据（用户、工作流、智能体等）时禁止删除并给出提示
  - 无关联数据时允许删除，删除前二次确认

---

### 14. 超级管理员删除用户
- **现状**：只能禁用用户，不能删除
- **需求**：超级管理员可软删除用户，删除前二次确认，处理关联数据避免数据孤儿

---

### 15. 用户分组（权限分组）
- **现状**：完全没有此功能
- **需求**：后台新增用户分组管理，分组作为权限标签，用于控制资源可见范围（与前台"我的分组"严格区分）

---

## P2 — 后续迭代

### 16. UI / UX 中度重设计
- 字号偏小，阅读不舒适
- 信息层级不强，重点不够突出
- 图标风格不统一
- 后台操作效率偏低，不像高效管理台
- 前台工作流区纵向拖沓感强
- 智能体展示模块折叠栏过弱
- **方向**：中度优化，不推翻重做，重点提升可读性、操作效率、信息层级清晰度

---

## 汇总

| 优先级 | 编号 | 项目 |
|--------|------|------|
| P0 | 1 | 术语全量替换（企业→组织） |
| P0 | 2 | 组织码规则升级（4~8位英文字母） |
| P0 | 3 | 用户类型与角色体系重构 |
| P0 | 4 | 固定三级组织结构（组织/部门/小组） |
| P0 | 5 | 注册入口与登录方式升级 |
| P0 | 6 | 权限与可见性模型升级 |
| P0 | 7 | 工作流步骤类型扩展（增加人工审核/其他） |
| P0 | 8 | 工作流可见权限操作优化（下拉选组织） |
| P0 | 9 | 首页联系二维码后台配置 |
| P0 | 10 | 列表页筛选功能补齐 |
| P1 | 11 | 批量操作（全选/取消） |
| P1 | 12 | 工作流后台分类管理 |
| P1 | 13 | 组织删除逻辑 |
| P1 | 14 | 超级管理员删除用户 |
| P1 | 15 | 用户分组（权限分组） |
| P2 | 16 | UI / UX 中度重设计 |

---

## 建议开发顺序

```
第一步（数据库基础）：第3、4、5条 → 新增字段和表结构
第二步（权限模型）：第6条 → 建立统一权限关系表
第三步（业务逻辑）：第7、8、9、10条 → 功能补齐
第四步（文案&规则）：第1、2条 → 术语替换和校验
第五步（效率增强）：第11~15条 → P1 项目
第六步（视觉优化）：第16条 → UI/UX
```
