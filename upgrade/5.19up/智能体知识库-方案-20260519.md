# 5.19up · 智能体知识库（RAG）· 方案

日期：2026-05-19
来源：用户提出 —— 给智能体搭建功能增加知识库
状态：**v2 · 已按小B评审修订（5/19）· 待小B复评后方可立项开工**
分支归属：知识库是上线后的独立大功能，**不并入今天 plan-b**；建议 pr-d/plan-b 进
master 后，从 master 新拉 `feature/kb-rag`。最终由用户 / 小B 定。

---

## 一、背景与目标

**背景**：管理员用「智能体搭建器」搭出来的智能体（`agents`，走 openai / 智谱大模型
API），目前只有系统提示词，没有"挂资料"的能力。用户希望给它加知识库 —— 上传文档、
对话时自动检索相关内容、让智能体基于资料回答。

**目标**：在搭建器里给智能体配「知识库」。一套标准 RAG：文档入库 → 切块 → 向量化 →
对话时按问题检索 top-K 片段 → 注入上下文。

---

## 二、范围（已与用户锁定）

| | 是否做 | 说明 |
|---|---|---|
| 搭建器搭的智能体（`agents`，openai / 智谱） | ✅ **做** | 本方案唯一目标 |
| 扣子 / Dify 等外部平台智能体 | ❌ 不做 | 它们平台自带知识库，去那边配 |
| 终端用户自建智能体（`user_agents`） | ❌ 不做 | 不在本期范围 |
| 知识库管理界面 | 管理后台 | 受众是管理员，新增 `/admin/knowledge-bases` |

> 一句话范围：**只有"用智能体搭建器搭出来的智能体"能绑知识库，知识库在管理后台维护。**

---

## 三、现状盘点

**已有、可直接复用：**
- **文档文本提取逻辑**已现成 —— `lib/trial-text-extract.ts` 的 `extractTextFromBuffer()`
  支持 PDF / docx / txt / md / csv / xlsx / pptx，含编码处理，解析逻辑不用重写。
  ⚠️ **但不能直接复用调用（小B finding 2）**：该函数有 `TEXT_MAX_CHARS = 30_000`
  截断，附件聊天够用，知识库入库只拿前 3 万字符会**悄悄丢掉大文档后半截**、用户难
  察觉。PR-2 要抽 KB 专用提取入口（`extractTextForKb()` 或加 `maxChars` 选项）：
  入库默认不截断 / 按更高上限，确实要截时明确提示。
- **文件上传**链路现成 —— Supabase Storage + magic bytes 校验 + 20MB 限制。
- 搭建器两段式：`agent_drafts`（草稿）→ publish → `agents`（发布版），草稿配置存
  `builder_config`（jsonb），发布时随之带过去。
- 大模型 API 的 key 已集中在 `model_providers`（API 管理）。

**完全没有、本期要建：**
- 全仓库无 `vector` / `pgvector` / `embedding` —— **RAG 基建从零搭**。
- 现有 `files` 表是按"单次对话"挂的，不是持久知识库，**不复用**。

---

## 四、整体设计

### 4.1 数据模型（新增 4 张表 + 1 个检索 RPC + 启用 pgvector）

⚠️ **本期全站统一一个 embedding 模型、一个向量维度**（小B finding 5）。`vector(N)`
的 N 在迁移里写死，**不支持不同知识库混用不同维度的模型**。`knowledge_bases.embedding_model`
只作记录（标明这批向量由哪个模型生成），不构成"每库可选不同模型"的能力。

```
knowledge_bases   知识库
  id / name / description / created_by_admin
  embedding_model            （记录用：这批向量由哪个模型生成；全站统一，见 D1）
  status / created_at / updated_at

kb_documents      知识库里的文档
  id / kb_id(FK→knowledge_bases, ON DELETE CASCADE)
  filename / file_type / storage_path
  status         pending | indexing | done | failed
  chunk_count / error_msg / created_at

kb_chunks         文档切出来的片段（带向量）
  id / document_id(FK→kb_documents, ON DELETE CASCADE) / kb_id
  chunk_index / content(TEXT) / token_count
  embedding  vector(N)        ← N = 全站统一维度（D1 定，迁移写死）
  created_at
  索引：HNSW 向量索引 ON embedding；普通索引 ON kb_id

agent_knowledge_bases   智能体 ↔ 知识库 绑定（关联表，小B finding 6 新增）
  agent_id(FK→agents, ON DELETE CASCADE)
  kb_id(FK→knowledge_bases, ON DELETE CASCADE)
  PRIMARY KEY (agent_id, kb_id)
```

**检索 RPC**（小B finding 3 新增，PR-1 一并设计）：
```sql
match_kb_chunks(p_kb_ids uuid[], p_query vector, p_top_k int, p_threshold float)
  → 返回 top-K 片段（content / 相似度 / document_id）
```
原因：Supabase / PostgREST 无法直接表达 `ORDER BY embedding <=> $query`，向量距离
排序必须封成 RPC，后端用 `db.rpc("match_kb_chunks", {...})` 调。

⚠️ **D1（embedding 模型）必须在 PR-1 写迁移之前拍板** —— 维度写死在 `vector(N)`，
模型没定迁移就动不了。

**智能体 ↔ 知识库绑定（小B finding 6 修订，不再只靠 jsonb）**：
- **草稿侧**：`agent_drafts.builder_config.knowledge_base_ids: string[]`（jsonb，
  搭建器里勾选的意图）。
- **发布侧**：发布 / 重发布时按 `builder_config.knowledge_base_ids` **全量同步**到
  `agent_knowledge_bases`（与可见范围 / 工作流的"全量替换"同口径）。
- 对话检索、知识库详情页"被哪些智能体引用"、删库时的关联清理 —— **都走
  `agent_knowledge_bases` 关联表**，不靠扫 jsonb。
- 删知识库：关联表 `ON DELETE CASCADE` 自动清绑定行；草稿 `builder_config` 里的
  脏 id 由搭建器 `load()` 按"知识库已不存在"过滤掉。

### 4.2 文档摄取流程（入库）

管理员往某个知识库上传文档时：

1. 文件传 Supabase Storage（复用现有上传，存 `kb/` 目录）。
2. 用 **KB 专用提取入口**取全文（**不按 3 万字符截断**，见第三节 finding 2）。
3. **切块**：按 ~500 token / 块、块间重叠 ~80 token 切分，尽量在段落 / 标题边界断开
   （`lib/kb/chunk.ts`，新建）。
4. **向量化**：对每个块批量调 embedding API（`lib/kb/embed.ts`，新建），拿到向量。
5. 写 `kb_chunks`；文档 `status` 走 `pending → indexing → done`，失败置 `failed`
   + `error_msg`。

⚠️ 摄取是耗时操作（大文档切几百块 + 几百次 embedding 调用）。本期方案：
**同步摄取 + 单文档大小限制 + 状态字段如实反映进度**；`status` 字段已为将来改后台
异步任务留好位置（D6）。

### 4.3 对话权限前置校验 + 检索接入（`chat` 路由）

⚠️ **必须先补一个既有安全缺口（小B finding 1）**：`app/api/agents/[id]/chat/route.ts`
现在只按 agent_code 取 agent、判 `enabled`，**不校验当前用户对该 agent 是否可见**
（`resource_permissions`）。挂知识库前，这是"猜到 code 就能聊"；挂知识库后会升级成
"猜到 code 就能问出知识库里的资料"—— info leak。**PR-5 必须先补 chat 前置可见性
校验，再接 RAG。**

`chat` route 新流程：

0. 取到 agent、判 `enabled` 后，**校验当前用户对该 agent 有访问权**（按
   `resource_permissions`，与前台智能体列表 / 工作流同一套可见性判断）。无权 → 403，
   不进入检索 / 对话。
1. 查该 agent 在 `agent_knowledge_bases` 里是否绑定了知识库。
2. 有 → 对**本轮用户问题**调 embedding 得到查询向量。
3. 调 `match_kb_chunks(绑定的 kb_ids, 查询向量, topK, 阈值)` RPC 取 top-K 片段（D4）。
4. 把检索到的片段拼成一段 system / context 消息，注入到发给 `streamChat()` 的
   messages 里（历史消息之前、当前问题之前）。
5. 其余流程（配额、扣费、SSE）不变。

- 无绑定知识库 / 非搭建器智能体 → 不走检索，零影响（但**第 0 步权限校验对所有
  agent 生效**）。
- 检索失败（embedding 调用挂 / RPC 报错）→ **降级**：当作无知识库正常回答，不阻断对话。
- `greeting` 路由开场白一般不需检索，本期不接（D7）。

### 4.4 知识库管理后台（新增 `/admin/knowledge-bases`）

- 知识库列表：增删改、看文档数 / 片段数。
- 进某个知识库：上传文档、文档列表（带 `status`）、删除文档、重建索引。
- 侧边菜单「内容」组加入口（与「智能体搭建」并列）。

### 4.5 搭建器集成（`agent-builder/[id]/page.tsx` + publish 路由）

- 5 分区 → **6 分区**，新增「知识库」分区。
- 该分区：一个多选，列出所有知识库，勾选的写进 `builder_config.knowledge_base_ids`。
- **发布逻辑要改**（小B finding 6）：发布 / 重发布时按 `builder_config.knowledge_base_ids`
  全量同步到 `agent_knowledge_bases` 关联表 —— 与「可见范围发布翻译」同模式。

---

## 五、PR 拆分

| PR | 内容 | 估时 |
|---|---|---|
| **PR-1** 向量基建 + 数据模型 | 迁移（启用 `pgvector` + 4 张表 + `vector` 列 + HNSW 索引 + `match_kb_chunks` 检索 RPC）；切块工具 `lib/kb/chunk.ts` | 2.5 天 |
| **PR-2** embedding + 文档摄取 | embedding 适配器 `lib/kb/embed.ts`；**KB 专用文本提取（不截断）**；摄取流程（上传→提取→切块→向量化→入库）+ 文档状态机 + 重建索引 | 3 天 |
| **PR-3** 知识库管理后台 | `/admin/knowledge-bases` 页 + 后端 API（KB CRUD、文档上传 / 列表 / 删除 / 重建、"被哪些智能体引用"反查）+ 菜单入口 | 3 天 |
| **PR-4** 搭建器集成 | 搭建器加「知识库」分区 + 绑定多选；发布时同步 `agent_knowledge_bases`；草稿脏 id 过滤；草稿 / 发布回归 | 2 天 |
| **PR-5** chat 权限校验 + 检索接入 | **先补 chat 前置可见性校验**（既有缺口）；再接条件检索 + 上下文注入 + 失败降级；非知识库智能体零影响回归 | 2.5 天 |
| 测试回归 | 全链路 + 跨境 Supabase 抖动 + token / 配额 + 越权场景 | 2 天 |

每个 PR 独立可验证。PR-1 必须等 D1 拍板后才能写迁移。

---

## 六、工时估算

| 项 | 估时 |
|---|---|
| PR-1 ~ PR-5 | 13 天 |
| 测试回归 | 2 天 |
| **合计** | **约 15 个工作日** |

> 算上与产品 / 小B 的方案来回和联调，**对外按 3.5 周报**（v2 比 v1 多了 chat 权限
> 校验、检索 RPC、关联表、不截断提取等实打实的活）。
> 不含「完整打磨版」（命中测试、引用来源展示、增量更新、异步摄取）—— 那些列在
> D6 / D7，若都要再加约 1 周。

---

## 七、决策点（请产品 / 小B 拍板）

| 编号 | 问题 | 方案推荐 |
|---|---|---|
| **D1** | embedding 模型选型（**决定向量维度，必须最先定**） | OpenAI `text-embedding-3-small`（1536 维，便宜、效果好）；若顾虑跨境用智谱 embedding。维度以所选模型文档为准 |
| **D1-2** | embedding 配置从哪来（小B finding 4 新增） | embedding 的 endpoint / 模型 / 返回结构与 chat completions 不同，**不能简单"复用 model_providers"**。三选一：① 复用某个 openai 大模型 API 的 key、但单独配 embedding endpoint；② API 管理加 `category=embedding`；③ 知识库模块级单独配一处 embedding 配置。**推荐 ③** —— 全站统一一处，最简、与"全站统一维度"一致 |
| D2 | 知识库归属 / 共享 + 谁能管理 | **全局共享** —— 任何搭建器智能体能绑任意知识库。但须明确**哪些管理员能创建 / 管理 / 绑定知识库**（建议限超管 / 系统管理员，org_admin 不可），否则全局共享叠加多租户会变成"租户资料串库"。本期不做创建者私有 / 租户隔离 |
| D3 | 一个智能体绑几个知识库 | **支持多个**（`knowledge_base_ids` 数组），检索时跨库取 top-K |
| D4 | 检索参数 | top-K = 5；可选相似度阈值（低于阈值的片段丢弃，防噪声）。先给默认值，后续可调 |
| D5 | 切块大小 | ~500 token / 块 + ~80 token 重叠，段落 / 标题边界优先 |
| D6 | 文档摄取同步还是异步 | **本期同步**（+ 单文档大小限制）；`status` 字段已留好，异步后台任务作为后续可选 |
| D7 | 检索结果给用户看「引用来源」 / greeting 是否接检索 | **本期都不做**，列入打磨版 |
| D8 | embedding 调用 + 检索增大的 prompt 的 **token / 配额口径** | 需明确：① embedding 调用本身的 token 现在不计入 `logs`；② 检索注入会让 prompt token 变大、被 W1 统计到。建议本期 embedding 不计费、只在文档维度记调用量，对话侧照 W1/W2 现状走 |

> ⚠️ **D1 是开工前置**：`vector(N)` 列维度在 PR-1 迁移里写死，模型没定就动不了 PR-1。

---

## 八、风险

- **越权访问知识库资料（小B finding 1，安全硬门槛）**：chat route 原本不校验 agent
  可见性 —— 挂知识库后等于"猜到 agent_code 就能问出库里资料"。PR-5 的前置可见性校验
  必过：无权用户 / 越权 code 一律 403，回归必测越权场景。
- **租户资料串库**：全局共享知识库下，若管理权限不收口，A 租户的智能体可能绑上含
  B 租户敏感资料的库。D2 须明确"谁能建 / 绑知识库"的边界。
- **跨境 Supabase**：摄取要写大量 `kb_chunks`、检索要做向量查询，跨境抖动会放大延迟 /
  失败。摄取须可重试、可重建索引；检索失败要降级（检索不到就当无知识库正常回答，
  绝不阻断对话）。
- **摄取耗时**：大文档同步摄取可能撞上部署环境的请求超时。PR-2 要确认部署环境
  （Jenkins / pm2 自托管 or Vercel）的超时上限，必要时限制单文档大小或分批。
- **检索质量**：切块大小、top-K、阈值直接影响"答得准不准"。PR-5 留可调参数，上线后按
  实际效果调，别一次写死。
- **`pgvector` 扩展**：需在 Supabase 后台启用 `vector` 扩展（用户操作）；HNSW 索引
  需 pgvector 0.5+，确认 Supabase 实例版本。
- **token / 配额（D8）**：检索注入让每次对话的 prompt 变大 —— 加权扣额度不受影响
  （按模型不按 token），但 W1 的 token 统计数字会上升，属预期，需在变更记录里说明。
- **对话链路改动**：`chat` route 是最高风险模块，PR-5 必须回归"非知识库智能体、
  扣子 / Dify 智能体"完全不受影响。

---

## 九、涉及文件

```
新建：supabase/migration_v38_knowledge_base.sql   pgvector + 4 张表 + HNSW 索引 +
                                                  match_kb_chunks 检索 RPC（编号续
                                                  v37，若 5.16up 方案B R6 已占 v38
                                                  则顺延，开工前确认）
新建：lib/kb/chunk.ts                             文档切块
新建：lib/kb/embed.ts                             embedding 适配器
新建：lib/kb/extract.ts                           KB 专用文本提取（不截断）
新建：lib/kb/retrieve.ts                          调 match_kb_chunks RPC 的检索封装
新建：app/api/admin/knowledge-bases/route.ts            知识库 列表 + 新建
新建：app/api/admin/knowledge-bases/[id]/route.ts       知识库 GET / PATCH / DELETE
新建：app/api/admin/knowledge-bases/[id]/documents/route.ts        文档 上传 + 列表
新建：app/api/admin/knowledge-bases/[id]/documents/[docId]/route.ts 文档 删除 + 重建
新建：app/admin/knowledge-bases/page.tsx                知识库列表页
新建：app/admin/knowledge-bases/[id]/page.tsx           知识库详情 / 文档管理 / 引用反查
修改：app/admin/agent-builder/[id]/page.tsx             加「知识库」分区
修改：app/api/admin/agent-drafts/[id]/publish/route.ts  发布时全量同步 agent_knowledge_bases
修改：app/api/agents/[id]/chat/route.ts                 加前置可见性校验 + 条件检索 + 注入
修改：components/layout/admin-layout.tsx                侧边菜单加「知识库」入口
新建：upgrade/5.19up/智能体知识库-方案-20260519.md       （本文件）
```

`lib/trial-text-extract.ts`：PR-2 把解析逻辑抽成"不截断"的公共入口供 KB 复用
（见 finding 2），可能小改。

---

## 十、小B 评审修订（v2 · 5/19）

第一轮小B 评审提 6 点，均已采纳，本文档升 v2：

| # | 小B finding | 处理 |
|---|---|---|
| 1 | chat route 不校验用户对 agent 的可见性 —— 挂知识库后"猜到 code 就能问出库里资料" | 采纳 · PR-5 改为"先补 chat 前置可见性校验、再接 RAG"（见 §4.3、§八、PR-5） |
| 2 | `extractTextFromBuffer()` 有 `TEXT_MAX_CHARS=30000` 截断，入库会丢大文档后半截 | 采纳 · 抽 KB 专用不截断提取入口 `lib/kb/extract.ts`（见 §三、§4.2、PR-2） |
| 3 | 向量距离排序不能直接写在 Supabase JS 查询里 | 采纳 · 新增 `match_kb_chunks` RPC，PR-1 一并设计（见 §4.1、§4.3、PR-1） |
| 4 | embedding 配置不能简单"复用 model_providers"（endpoint / 结构不同） | 采纳 · 新增决策点 D1-2（embedding 配置来源） |
| 5 | 每库存 `embedding_model/dim` 与全局固定 `vector(N)` 语义冲突 | 采纳 · §4.1 声明"本期全站统一一个 embedding 模型 / 维度"，去掉 `embedding_dim` |
| 6 | `builder_config.knowledge_base_ids` 不建关联表 → 删库脏 id / 反查难 | 采纳 · 新增 `agent_knowledge_bases` 关联表，发布时全量同步（见 §4.1、§4.5） |

小B 结论：方向可立项，但**权限校验 / 提取截断 / 检索 RPC 三点是硬门槛**，v2 修订后再开工。

---

## 十一、待小B复评

1. v2 三处硬门槛修订（§4.3 权限校验、§4.2 不截断提取、§4.1 检索 RPC）是否到位。
2. 新增 / 调整的决策点：**D1-2（embedding 配置来源）**、**D2（管理权限边界，防串库）**。
3. PR 拆分与第六节估时（v2 升到约 15 人日 / 3.5 周）是否认可。
4. 第八节风险，尤其越权访问、租户串库、摄取耗时与部署环境超时、检索失败降级。
