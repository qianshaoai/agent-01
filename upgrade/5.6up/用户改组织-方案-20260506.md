# 后台修改用户所属组织方案

**文档日期**：2026-05-06
**状态**：草案，等评审拍板后开工
**适用范围**：`/admin/users` 编辑用户时允许修改 `tenant_code`，含个人↔组织、组织↔组织双向转移

---

## 一、需求原文对齐

> 「在后台给个人用户加入组织」

需求拆解 → 4 个决策点（已确认）：

| 决策 | 选项 | 已选 |
| --- | --- | --- |
| 改 tenant_code 方向 | A 仅个人→组织 / **B 双向** / C 完全自由 | **B** |
| 历史数据归属 | i 跟人走 / **ii 跟组织走（logs.tenant_code 追溯改）** | **ii** |
| 配额规则 | 转入后从组织配额消耗 | ✓ |
| 是否强制重登 | yes（cookie 失效） | ✓ |

---

## 二、关键事实

### users.tenant_code 现状

```
schema.sql:27  tenant_code TEXT NOT NULL DEFAULT 'PERSONAL'
```

无 FK（schema 里没看到 REFERENCES）。值含义：
- `'PERSONAL'` = 个人用户
- 其它字符串 = 组织码（应为 `tenants.code` 中存在的某条）

### 受影响的表（决策 ii 下）

| 表 | 处理 |
| --- | --- |
| `users.tenant_code` | 改值（必做）|
| `logs.tenant_code` | **追溯改**（该 user_id 的所有 logs 改成新组织码）|
| `users.force_relogin_at` | 写 NOW() 强制重登（必做）|
| 其它（conversations / messages / files / departments / teams / resource_permissions / tenant_agents 等）| **不动**——这些不带 user 维度 tenant_code，或与单个 user 转组织无关 |

### 配额流转

- 改前用户消耗"个人配额"（取决于业务规则——若个人无独立 quota 表则不消耗）
- 改后用户的 chat 调用走 `tenants.quota / quota_used` 共享池（chat route 现有逻辑）
- **不补/不退个人剩余配额**——直接切换归属

### Cookie 失效

跟"组织码可改"方案同一套机制：用户表加 `force_relogin_at`，中间件验 token 时若 `iat < force_relogin_at` 视为失效。差别是这次只对**单个用户**触发，不需要触及 admin token / 同组织其它人。

---

## 三、改动范围

### 允许改

| 路径 | 改动 |
| --- | --- |
| `supabase/migration_v23.sql` | 新建。给 `users` 加 `force_relogin_at TIMESTAMPTZ`（如未存在）；新增 rpc `change_user_tenant(user_id UUID, new_tenant_code TEXT)`，事务内做 users 改 + logs 追溯改 + force_relogin_at 写入 + 一条 audit logs |
| `app/api/admin/users/[id]/route.ts` | PATCH 加 `tenantCode` 字段；调 rpc；权限校验 |
| `app/admin/users/page.tsx` | 编辑用户弹窗加"所属组织"下拉（'个人' + 当前可见组织）+ 二次确认 + 警告文案 |
| `lib/auth.ts` | getCurrentUser 加 `iat < force_relogin_at` 检查 |
| `middleware.ts` | 失效时清 cookie + 重定向登录 |

### 不改

| 路径 | 原因 |
| --- | --- |
| 其它表 / migration | 决策 ii 下只动 logs，其它不动 |
| `app/api/admin/users/route.ts` POST（创建用户）| 创建时已经支持指定 tenant_code，不需要改 |
| `lib/db.ts` / `lib/adapters/**` | 无关 |
| `app/agents/**` / `app/page.tsx` 等用户端 | 用户端无 UI 改动 |

---

## 四、实施细节

### 1. RPC 函数

```sql
CREATE OR REPLACE FUNCTION change_user_tenant(
  p_user_id UUID,
  p_new_tenant_code TEXT
) RETURNS VOID
LANGUAGE plpgsql
AS $$
DECLARE
  v_old_tenant_code TEXT;
  v_user_phone TEXT;
BEGIN
  -- 校验目标组织存在或为 PERSONAL
  IF p_new_tenant_code <> 'PERSONAL'
     AND NOT EXISTS (SELECT 1 FROM tenants WHERE code = p_new_tenant_code) THEN
    RAISE EXCEPTION 'tenant_code "%" not found', p_new_tenant_code;
  END IF;

  -- 取旧值
  SELECT tenant_code, phone INTO v_old_tenant_code, v_user_phone
  FROM users WHERE id = p_user_id;
  IF NOT FOUND THEN
    RAISE EXCEPTION 'user % not found', p_user_id;
  END IF;
  IF v_old_tenant_code = p_new_tenant_code THEN
    RAISE EXCEPTION 'user % already in tenant %', p_user_id, p_new_tenant_code;
  END IF;

  -- 1. 改 users
  UPDATE users
     SET tenant_code = p_new_tenant_code,
         force_relogin_at = NOW()
   WHERE id = p_user_id;

  -- 2. 追溯改 logs（决策 ii）
  UPDATE logs
     SET tenant_code = p_new_tenant_code
   WHERE user_phone = v_user_phone
     AND tenant_code = v_old_tenant_code;

  -- 3. 审计：写一条变更事件
  INSERT INTO logs (user_phone, tenant_code, action, status, error_msg)
  VALUES (
    v_user_phone,
    p_new_tenant_code,
    'admin_change_user_tenant',
    'success',
    'from ' || v_old_tenant_code || ' to ' || p_new_tenant_code
  );
END;
$$;
```

### 2. PATCH 接口

```ts
// app/api/admin/users/[id]/route.ts
export async function PATCH(...) {
  const admin = await requireAdmin();
  if (admin instanceof Response) return admin;
  const { tenantCode, ...other } = await req.json();

  if (typeof tenantCode === "string") {
    // 校验：format / 不能为空 / 长度 / 与现有组织/PERSONAL 一致
    const { error } = await db.rpc("change_user_tenant", {
      p_user_id: userId,
      p_new_tenant_code: tenantCode,
    });
    if (error) return NextResponse.json({ error: error.message }, { status: 400 });
  }

  // 其它字段照常更新（nickname / role 等）
  ...
}
```

### 3. UI · 编辑表单

`/admin/users` 编辑用户 modal 里加一个"所属组织"区域：
- 下拉选项：
  - "个人空间（PERSONAL）"
  - 该 admin 可见的所有组织（按 `tenants` 列表 + 当前 admin 角色权限过滤）
- 当前值高亮选中
- 改了选项后保存 → 弹二次确认：
  - 文案："确认把用户 `{nickname / phone}` 从 `{old}` 转入 `{new}`？此操作会：1) 切换该用户的所属组织和配额来源；2) 历史日志的归属一并迁移到新组织；3) 该用户当前登录态会失效，必须重新登录。"
  - 按钮："取消" / "确认转移"
- 保存调 PATCH，成功后刷新列表 + toast"用户已转入 {new}，将在该用户下次访问时被强制重登"

### 4. 失效检查

- `users` 加 `force_relogin_at TIMESTAMPTZ`（如未存在）
- `lib/auth.ts` `getCurrentUser`：解 JWT 拿 iat → 查 user.force_relogin_at → 比较：
  ```ts
  if (user.force_relogin_at && new Date(token.iat * 1000) < new Date(user.force_relogin_at)) {
    return null; // 视为未登录
  }
  ```
- `middleware.ts` 沿用 `getCurrentUser` 失败的处理路径——清 cookie + 重定向登录

### 5. 权限隔离

- super_admin / system_admin：可任意改用户的 tenant_code
- org_admin：**仅能在自己组织内操作**——
  - 不允许把别的组织的用户转进自己组织（接口侧拦截）
  - 不允许把自己组织的用户转出（防止把人甩走）
  - 不允许操作 PERSONAL 用户（个人用户由系统级管理员处理）

最简实现：**org_admin 不显示"所属组织"下拉**，只 super/system_admin 能看到。

---

## 五、验收清单

### 基础

| 编号 | 验收点 |
| --- | --- |
| U1 | super_admin 进 `/admin/users` 编辑某用户 → 看到"所属组织"下拉，能选个人 / 组织 |
| U2 | 选了新值 → 弹二次确认，列出 old → new 文案 |
| U3 | 取消 → 不改 |
| U4 | 确认 → 调 PATCH 成功，列表里该用户的 tenant_code 显示新值 |
| U5 | 选当前值不变 → PATCH 报错"already in tenant"或前端禁用确认按钮 |
| U6 | 选不存在的 tenant_code → PATCH 报错"tenant not found" |

### 配额流转

| 编号 | 验收点 |
| --- | --- |
| U7 | 改前用户对话 / 改后用户对话 → 配额来源切到新组织（chat route 走 tenant.quota） |
| U8 | DB 查 `tenants.quota_used` 在该用户改后用了一次能正确 +1 |

### 历史数据（决策 ii）

| 编号 | 验收点 |
| --- | --- |
| U9 | SQL 查 `logs WHERE user_phone = X` → 全部 tenant_code 已改成新组织 |
| U10 | logs 里能看到一条 `action='admin_change_user_tenant'` 的 audit 事件，error_msg 含"from X to Y" |

### 强制重登

| 编号 | 验收点 |
| --- | --- |
| U11 | 该用户改前登录的浏览器 → 改后下次任何请求 → 被中间件清 cookie，跳登录页 |
| U12 | 该用户重新登录后，cookie 里 tenantCode = 新值 |
| U13 | 同时**不影响其它用户**：同组织其它人 cookie 仍有效 |

### 权限隔离

| 编号 | 验收点 |
| --- | --- |
| U14 | super_admin / system_admin 能编辑任何用户的 tenant_code |
| U15 | org_admin 编辑用户表单**不显示**"所属组织"下拉（或显示但禁用） |
| U16 | 直接 curl 用 org_admin 的 cookie 调 PATCH 加 tenantCode → 接口侧拒绝 |

### 容错

| 编号 | 验收点 |
| --- | --- |
| U17 | rpc 中途失败（比如手动 raise）→ 整个事务回滚，DB 状态不变 |
| U18 | npm run lint / npx tsc --noEmit / npm run build 通过 |

---

## 六、风险与回退

### 风险

1. **强制重登的体验**：被转用户正在工作时被踢——需要让 admin 操作前**自行通知**该用户。前端二次确认文案明确写"会强制重登"。
2. **logs 追溯改是不可逆的**：如果改错了用户，再改回去 logs 也会跟着回去，但中间已经写过的"audit 事件"会留两条记录形成历史。可接受。
3. **org_admin 误操作**：本方案排除 org_admin 改 tenant_code 的能力，从源头规避。
4. **PERSONAL 用户配额**：转入组织后该用户开始消耗组织配额。如果业务上有"个人用户独立计费"的需求，本方案不处理（个人用户当前没独立配额表）。

### 回退

- 操作错误：再调一次 rpc 把 tenant_code 改回去（rpc 可重入）
- 灾难：rpc 函数有 bug → supabase PITR 恢复
- 代码层：本方案改动集中在 5 个文件，删除新增的 PATCH 分支 / UI 区块即可还原；rpc 函数可保留不删

---

## 七、不做

- 不做"批量转移用户"
- 不做"用户多组织成员身份"（一个 user 只对应一个 tenant_code）
- 不做"配额迁移 / 退款 / 补差"
- 不做 personal 独立配额体系
- 不做被转用户的邮件/短信通知（admin 操作前人肉通知）
- 不做组织码可改方案里的全表级联（本方案不动 tenant_agents / departments / teams / resource_permissions / 等等，因为那些是"组织"维度的关联，跟单个用户转组织无关）

---

## 八、工作量

- migration_v23.sql + rpc：1.5h
- 后端 PATCH + auth 失效检查：1.5h
- admin UI 编辑表单 + 二次确认：1.5h
- 权限隔离逻辑 + 接口侧拦截 org_admin：1h
- 真机回归 U1-U18：1.5h
- **合计 1 天**

---

## 九、决策点（已确认）

- [x] 双向（B）
- [x] 历史 logs 追溯改（ii）
- [x] 转入后从组织配额消耗
- [x] 强制重登

待评审通过后开工。

---

## 十、本文档定位

草案。等评审 / 拍板。
