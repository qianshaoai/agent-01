# 5.15up 变更记录（merge 合并版 · 5/20）

> 同一天 5/15 有两条并行工作记录，本文档**两段全保留**：
> - **A 主线 · 智能体搭建器 PR-A → PR-D 全部完工**（来自 devC 分支）
> - **B 旁支 · 上传附件 PDF / 图片识别排查与修复**（来自 master 分支）

---

# A 主线 · 智能体搭建器（Day 2 · devC 分支）

# 5.15up 变更记录（Day 2 · 2026-05-15）

承接 [变更记录-20260514.md](./变更记录-20260514.md) Day 1 的方案 + 桌面 docx 文档工作，
今天完成 5.15up 智能体搭建器 **PR-A → PR-D 4 个 PR 全部代码就位 + 本地验证通过**。

---

## 一、PR 时间线

| 时间 | PR | 内容 | 状态 |
|---|---|---|---|
| 5/14 17:00 | PR-A | 模型 API 管理（model_providers 表 + CRUD + 测试连通性 + UI） | ✅ 推 origin / 本地验证 |
| 5/14 17:30 | PR-B | 智能体搭建器骨架（agent_drafts 表 + 草稿 CRUD + 编辑 UI 5 个分区） | ✅ 推 origin / 本地验证 |
| 5/15 09:30 | PR-C | 测试聊天（SSE）+ 发布到 agents（默认 enabled=false） | ✅ 推 origin / 本地验证 |
| 5/15 10:30 | PR-D | chat / greeting 兼容 provider（三级 fallback 解析） | ✅ 推 origin / 本地验证 |

**5.15up 智能体搭建器主线完工 ✅**

---

## 二、关键代码改动

### 2.1 数据库迁移（用户已跑 Supabase Dashboard）

| 编号 | 内容 |
|---|---|
| `migration_v34_logs_status_aborted.sql` | `logs.status` CHECK 加 'aborted'，顺手修了 chat aborted 静默丢日志的 silent bug |
| `migration_v35_model_providers.sql` | 新表：编号/名称/平台/endpoint/加密 key/默认模型/默认参数/启停 + enabled/platform 双索引 |
| `migration_v36_agent_drafts.sql` | 新表 + agents 表加 3 列（provider_id / builder_config / published_from_draft_id） |

### 2.2 后端新增 API

```
/api/admin/model-providers/                  POST list + GET CRUD
/api/admin/model-providers/[id]/             GET / PATCH / DELETE
/api/admin/model-providers/[id]/test/        POST 连通性测试（10s 超时 + 错误脱敏）
/api/admin/agent-drafts/                     POST + GET list
/api/admin/agent-drafts/[id]/                GET / PATCH / DELETE（DELETE 加 error 检查 bugfix）
/api/admin/agent-drafts/[id]/duplicate/      POST 复制草稿
/api/admin/agent-drafts/[id]/test-chat/      POST SSE 测试聊天（30s 首字节超时 + 错误脱敏）
/api/admin/agent-drafts/[id]/publish/        POST 发布（upsert + 强制 enabled=false）
```

### 2.3 后端 chat / greeting 路由改造（PR-D 核心）

`app/api/agents/[id]/chat/route.ts`：
- 三级 fallback：agent 自带 key → provider_id → tenant.openai_key_enc
- 合并 modelParams: `provider.default_params < agent.model_params`
- 失败路径友好文案（不暴露 decrypt failed / 内部错误）

`app/api/agents/[id]/greeting/route.ts`：
- 优先返回 `builder_config.opening_message + suggested_questions`
- 没有再走原 Coze 平台 prologue 路径

### 2.4 前端

```
/admin/model-providers/page.tsx              完整 CRUD UI + 行内测试连接 + 平台联动默认 endpoint/model
/admin/agent-builder/page.tsx                草稿列表 + 列表 retry 3 次
/admin/agent-builder/[id]/page.tsx           5 分区编辑表单 + 右侧测试聊天面板（PR-C）+ 发布弹窗（PR-C）
/admin/agents/page.tsx                       新增"启用/禁用"切换按钮（5.15up PR-D 配套）
components/layout/admin-layout.tsx           "内容"组新增"模型接入" + "智能体搭建" 入口
```

---

## 三、dev 稳定性补丁（开发期跨境 Supabase 抖动）

### 3.1 lib/db.ts 加 retry wrapper（根治）

```ts
async function retryingFetch(input, init) {
  for (let i = 0; i < 3; i++) {
    try { return await fetch(input, init); }
    catch (e) { if (i === 2) throw e; await new Promise(r => setTimeout(r, 200 * (i+1))); }
  }
}
const db = createClient(..., { global: { fetch: retryingFetch } });
```

所有 `db.from(...)` 内部自动 retry 3 次 / 200ms / 400ms 退避，只对 fetch throw（ECONNRESET / TLS handshake 失败）触发，HTTP 4xx/5xx 不重试。

### 3.2 前端列表 retry 3 次（缓解 dev 严格模式双调）

`/admin/model-providers/page.tsx` + `/admin/agent-builder/page.tsx` 的 `loadList` 改 3 次重试，全失败才 flash err，避免 React Dev 严格模式 useEffect 双调时 mount-1 失败 mount-2 成功导致红条残留。

### 3.3 修一类隐藏 bug：fetch failed 被当 NOT_FOUND

| 路由 | 现象 | 修法 |
|---|---|---|
| `/api/admin/agent-drafts/[id]` DELETE | 删除报"草稿不存在"但 DB 里有 | 加 error 检查，区分 DB 故障和真不存在 |
| `/api/agents/[id]/chat` (5.15up bugfix) | 聊天报"组织账号已禁用"但 tenants 表里 enabled=true | `.single() → .maybeSingle()` + 加 error 检查，503 返回"加载组织信息失败" |

→ 这是个**类问题**：项目里所有 `const { data } = await db.from(...).single(); if (!data) return NOT_FOUND` 模式都可能有这 bug。后续遇到逐个修。

---

## 四、dev 工具

`scripts/dev-reset-admin.mjs`：
- 一行命令 `node scripts/dev-reset-admin.mjs` 把 admin 重置回 admin/admin
- 起因：Fix 2 改密测试副作用频繁污染 admin 账号，每次走 SQL 太重

---

## 五、本地验证记录

| PR | 验证项 | 结果 |
|---|---|---|
| PR-A | 后台新增智谱 GLM 供应商 + 测试连通性 | ✅ 1 秒返回成功 |
| PR-A | 平台切换时 endpoint/model 联动默认值 | ✅ |
| PR-A | 编号过滤中文输入 | ✅ |
| PR-B | 新建/编辑/复制/删除草稿 | ✅ |
| PR-B | 编辑页 5 分区表单数据回填 | ✅ |
| PR-C | 右侧测试聊天发"你好你好" → 智谱 glm-4-flash 流式回复 | ✅（一次性通过）|
| PR-C | 发布到 agents 表 → enabled=false 默认禁用 | ✅ |
| PR-C | 重发布同一草稿不创建新 agent | （未单独测，按代码逻辑 OK） |
| PR-D | admin/agents 切换按钮启用 PR-C 发布的 agent | ✅ |
| PR-D | 前台用普通用户聊新发布的智能体 → 走 model_providers 取 key | ✅ 第一次回复成功 |

**5.15up 智能体搭建器完整闭环验证通过** ✅

---

## 六、Git 分支结构

```
docs-5.15up-plans
  └─ docs(5.15up) 三份方案 + 14号变更记录 + 桌面 docx 改写记录

fix-3-encryption-key
  └─ Fix 3 路径 A 代码 + Jenkinsfile 注入 ENCRYPTION_KEY
     （已 push，等可发布时合并；Jenkins 凭据已加）

feature/agent-builder-pr-a
feature/agent-builder-pr-b
feature/agent-builder-pr-c
feature/agent-builder-pr-d                   ← 本次主线，所有 4 个 PR 累计在此分支
```

---

## 七、下一步

### 待办

1. **合并 5.15up 各 PR 到 master**（等 Fix 3 一并部署）
2. PR-E（打磨）：UI 响应式 / 错误状态优化 / 全量回归（方案 §3 PR-E 范围）
3. 修剩余"fetch failed → NOT_FOUND"模式（项目里还有几处类似 `single() + !data → 报不存在` 没改 error 检查）

### 已知警告 / 待办

- React Dev 严格模式 useEffect 双调让 dev 期红条偶发——前端已加 retry 缓解，prod 不受影响
- Turbopack 内部缓存在 `.next` 被外部删时会脏，需要 kill dev server 后再删 .next（已踩过坑）
- 5.13up 收口方案登记的 `migration_v34_logs_status_aborted` 已在 5.15up 一并实施
- Fix 3 加密 key 拆分：代码已就位 + Jenkins 凭据已加 + Jenkinsfile 改了；等可发布时一并 deploy

---

## 八、API 管理模块 PR-1（5/15 下午）

方案见 [API管理模块-方案-20260515.md](./API管理模块-方案-20260515.md)，D1-D5 已拍板，
分 PR-1~PR-4 实施。本节为 **PR-1：model_providers 加 `category` + UI 两 tab**。

### 8.1 数据库迁移

`migration_v37_model_providers_category.sql`（**需用户在 Supabase Dashboard 执行**）：

- `model_providers` 加列 `category TEXT NOT NULL DEFAULT 'model'`
- 存量数据按 platform 归类：`coze/dify/yuanqi/qingyan` → `category='agent'`
- 加 CHECK 约束 `category IN ('model','agent')`
- 加索引 `(category, enabled)`、`(category, platform)`
- 全部 IF [NOT] EXISTS / DROP IF EXISTS，可安全重跑

### 8.2 后端

`app/api/admin/model-providers/route.ts`：

- 新增 `CATEGORY_PLATFORMS` 映射（model=openai/zhipu，agent=coze/dify/yuanqi/qingyan）
- `GET` 支持 `?category=model|agent` 过滤
- `POST` 接收并校验 `category`，且校验 platform 与 category 匹配，写入 `category`

`app/api/admin/model-providers/[id]/route.ts`：

- `PATCH` 接收 `category`；当 platform / category 任一变更时，反查当前行校验二者匹配

### 8.3 前端

`app/admin/model-providers/page.tsx`：

- 页面标题「模型接入」→「API 管理」
- 顶部加两个 tab：大模型 API / 智能体 API（带数量角标），按 `category` 客户端过滤列表
- 新增 / 编辑表单带 `category`：当前 tab 决定新建类型，平台下拉只列该类平台
- 弹窗标题随 category 显示「新增大模型 API」/「编辑智能体 API」

`components/layout/admin-layout.tsx`：

- 侧边菜单「模型接入」→「API 管理」

### 8.4 验证

- `npx tsc --noEmit` 通过
- 待用户执行 `migration_v37` 后本地验证：两 tab 切换、各 tab 新建 / 编辑、
  平台下拉按类过滤、跨类校验报错（如智能体 tab 选 openai 应被拒）

### 8.5 涉及文件

```
新建：supabase/migration_v37_model_providers_category.sql
修改：app/api/admin/model-providers/route.ts
修改：app/api/admin/model-providers/[id]/route.ts
修改：app/admin/model-providers/page.tsx
修改：components/layout/admin-layout.tsx
```

### 8.6 PR-1 后续

- PR-2：智能体「API 配置」弹窗下拉引用 + agents 接口 `provider_id` + chat route 改 `provider_id` 优先
- PR-3：数据迁移脚本
- PR-4：移除旧手输 API Key 入口

---

## 九、API 管理模块 PR-2（5/15 下午）

**智能体引用命名 API + chat route 优先级修正**。无需 migration（`agents.provider_id`
列 migration_v36 已加）。

### 9.1 chat route 配置解析优先级翻转（小B 评审核心修正）

`app/api/agents/[id]/chat/route.ts`：

- 原优先级 `agent.api_key_enc → provider_id → tenant openai` —— 隐患：迁移回填
  `provider_id` 后只要旧 `api_key_enc` 还在，聊天永远走旧 key，API 管理更新不生效。
- 改为：
  1. `agent.provider_id` 非空 → **强制**走 `model_providers`；provider 删除 / 禁用 /
     无 key / endpoint 空 / 解密失败一律**阻断报错，不回退旧 key**
  2. `provider_id` 空 + `api_key_enc` 存在 → 用旧 agent 自带 key（未迁移历史 agent）
  3. 仍无 + `platform=openai` + 租户 `openai_key_enc` → 兜底
  4. 都没有 → 503

### 9.2 后端 agents 接口

`app/api/admin/agents/route.ts` GET：

- `select` 加 `provider_id`
- 批量反查 `model_providers`，每个 agent 附 `provider: { name, category, platform, enabled }`

`app/api/admin/agents/[id]/route.ts` PATCH：

- 接收 `providerId`：空 → 解绑（`provider_id = null`）；非空 → 校验 provider
  存在 + 启用 + category 与 agent 平台匹配（coze/dify/yuanqi/qingyan→agent，其余→model）

### 9.3 前端 智能体管理「API 配置」弹窗

`app/admin/agents/page.tsx`：

- 主操作改为「命名 API」下拉，按 agent 平台类别拉 `/api/admin/model-providers?category=`
- 旧「API Endpoint / API Key」手输入口移除，改为**只读「历史配置」卡片**（显示历史
  接口地址 + 历史 Key 状态 + 迁移提示）—— 对应 D5 分阶段：PR-2 先下拉化
- 加「去 API 管理新建 / 更新 API」快捷链接
- 模型参数 JSON 仍可编辑；保存只提交 `providerId` + `modelParams`
- 保存失败弹后端错误文案（原来静默吞错）

### 9.4 验证

- `npx tsc --noEmit` + eslint 通过
- 待本地验证：
  - 智能体 API 配置弹窗能选命名 API、下拉按平台类别过滤
  - 绑定后聊天走命名 API；在 API 管理改该 key → 立即生效
  - provider 禁用/删除后，已绑定 agent 聊天阻断报错（不走旧 key）
  - 未绑定 agent 仍用历史 key 正常聊天

### 9.5 涉及文件

```
修改：app/api/agents/[id]/chat/route.ts
修改：app/api/admin/agents/route.ts
修改：app/api/admin/agents/[id]/route.ts
修改：app/admin/agents/page.tsx
```

### 9.6 PR-2 后续

- PR-3：数据迁移脚本（解密 fingerprint 合并 + 回填 `provider_id`，dry-run / 可重跑）
- PR-4：迁移验证通过后移除「历史配置」只读卡片

---

## 十、API 管理模块 · PR-1 微调（用户反馈）

用户反馈：编辑「智能体 API」时「默认模型 / 默认参数（JSON）」两个字段没意义 ——
智能体 API（Coze/Dify/元器/清言）模型与参数都在平台侧 bot 上配置，这里只是一个
平台凭证（endpoint + key）。

`app/admin/model-providers/page.tsx`：

- 新增 / 编辑弹窗：「默认模型」「默认参数（JSON）」两字段仅 `category === "model"`
  （大模型 API）时显示；智能体 API 不显示
- 列表表格：「默认模型」列仅「大模型 API」tab 显示，「智能体 API」tab 隐藏该列

不涉及后端 / DB —— 字段仍在 `model_providers` 表，智能体 API 存空值。

### 智能体 API 去掉「测试」按钮

用户反馈：智能体 API 点「测试」报「上游返回空响应」。

根因：「测试」是发一条真实对话验证连通，但 Coze/Dify 等对话必须带
`bot_id`/`assistant_id`，而它在**智能体**上、不在凭证记录里 —— 凭证层没有
合法 bot_id，测试时拿 `provider_code` 当 bot_id 发出去，上游不认 → 空响应。

修复（用户选方案 A —— 凭证层本就测不了对话）：

- `app/admin/model-providers/page.tsx`：「测试」按钮仅「大模型 API」tab 显示
- `app/api/admin/model-providers/[id]/test/route.ts`：后端兜底 —— `category='agent'`
  直接拒绝并提示「请在绑定该 API 的智能体里发消息验证」

智能体 API 的连通性 = 去「智能体管理」绑好该 API、发条消息能回即通。

---

## 十一、API 管理模块 PR-3 · 数据迁移脚本

新建 `scripts/migrate-agent-keys-to-providers.mjs` —— 把现存正式智能体自带的
`api_key_enc` 归拢成命名 API（`model_providers`），回填 `agents.provider_id`。
**纯脚本，不改应用代码、不改 schema。**

### 11.1 设计

- **分组**：逐条解密 agent 的 `api_key_enc` → 算 HMAC-SHA256 fingerprint
  （不能按密文分组，AES-GCM 随机 IV 同 key 密文每次不同）。
  分组维度 = `category + platform + 有效 endpoint + key fingerprint`。
- `bot_id` / `assistant_id` / `model` 等留在 `agent.model_params`，不进分组、不丢。
- endpoint 为空时按 platform 补默认值（避免 PR-2 chat route 的 endpoint 非空校验拦截）。
- **幂等**：`provider_code` 稳定（`migrated-{cat}-{platform}-{endpointHash6}-{fp8}`）；
  已存在则复用；已回填 `provider_id` 的 agent 不重复处理（`.is(provider_id, null)` 守卫）。
- `provider_code` 已存在但内容不一致 → 冲突，中止、不写入、要求人工排查。
- 只新增 `model_providers` + 回填 `provider_id`，**不删 `api_key_enc`**（过渡期保留）。
- 日志不输出明文 key / 完整 fingerprint。
- 默认 **dry-run**（只预览不写）；加 `--apply` 才正式执行。

### 11.2 用法

```
node scripts/migrate-agent-keys-to-providers.mjs            # dry-run 预览
node scripts/migrate-agent-keys-to-providers.mjs --apply    # 正式执行
```

### 11.3 Dry-run 结果（5/15 当前库）

```
agents 总数 44：已有 provider_id 2 / 无 api_key_enc 19 / 问题 0 / 待迁移 23
→ 归并为 8 条命名 API（最大一组：15 个 Coze 智能体共用 1 个 key）
```

「无 api_key_enc 19 个」= 外链型 / 未配置的 agent，本就无 key 可迁，跳过即可。

### 11.4 执行记录

- 5/15 用户授权后 `--apply` 正式执行：**新建 8 条命名 API，回填 23 个 agent.provider_id**。
- 核对：`model_providers` 多 8 条 `migrated-` 记录（全启用）；`agents` 44 个里 25 个
  已绑 `provider_id`（23 迁移 + 2 搭建器原有）；`api_key_enc` 保留未删。
- 用户把最大一组（15 个 Coze 智能体共用）的命名 API 改名为 `coze-main api`。

---

## 十二、API 管理模块 PR-4 · 收口

迁移验证通过后移除「API 配置」弹窗里的旧手输配置残留。

`app/admin/agents/page.tsx`：

- 移除「历史配置（只读）」卡片（PR-2 过渡期用，显示历史 endpoint / key 状态）
- 「命名 API」下拉「未绑定」选项文案去掉「使用下方历史配置兜底」（卡片已删）
- 加一行说明：「API Key 统一在『API 管理』里命名维护，此处选择引用即可」

至此「API 配置」弹窗只剩：命名 API 下拉 + 去 API 管理链接 + 模型参数 JSON。
旧 `api_key_enc` 列仍在表里（D4：本次不删，下个版本清），但 UI 已无任何手输入口。

**API 管理模块 PR-1 ~ PR-4 全部完成。** 后续：`api_key_enc` 列择期清理（D4）。

---

## 十三、小B 验收反馈修复（5/15）

小B 验收结论"暂不通过"，5 项 finding 逐项修复如下。

### High · model-providers DELETE 引用检查失效

`app/api/admin/model-providers/[id]/route.ts`：

- 原代码 `const { data: refAgents } = ...select(.., {count, head:true})` 后读
  `refAgents.count` —— `head:true` 时行数在响应的 `count` 字段、**不在 data 里**，
  `data` 恒为 null，引用检查形同虚设 → 被引用的 provider 可被硬删，外键
  `ON DELETE SET NULL` 把 agent 静默解绑、回落旧 key，绕过集中管理。
- 改为解构 `{ count, error }`，`count > 0` 阻断；agents / agent_drafts 各查一次，
  查询 error 也阻断（不静默放行删除）。

### Medium · greeting 路由未走 provider_id 优先

`app/api/agents/[id]/greeting/route.ts`：

- 原 Coze 路径直接 `decrypt(agent.api_key_enc)` —— provider 更新/禁用/清空后
  仍用旧 key 拉开场白，绕过集中管理；旧 key 过期则开场白静默丢失。
- 改为与 chat route 对齐：`provider_id` 非空时**严格**从 `model_providers` 取 key；
  provider 删除/禁用/无 key/解密失败 → 返回无开场白 + warn，**不 fallback 旧 key**。

### Medium · 改平台时未校验/清理 provider_id

`app/api/admin/agents/[id]/route.ts` PATCH：

- 原来改 `platform` 不动 `provider_id`，会出现 platform=openai 却绑着智能体类
  provider 的不一致状态。
- 修复：providerId 校验改用**变更后**的有效平台；只改平台没动 provider 时，
  若已绑 provider 的 category 与新平台不符 → 自动解绑 `provider_id`，
  响应带 `providerUnbound:true`，前端 toast 提示重新配置。

### Low · PATCH 不再接受 apiKey 写 api_key_enc

`app/api/admin/agents/[id]/route.ts`：移除 `if (body.apiKey) updates.api_key_enc = encrypt(...)`
及 `encrypt` 导入 —— 收口期不再保留"手输/覆盖旧 key"的接口入口。

### Low · MIGRATIONS.md 登记迁移

`supabase/MIGRATIONS.md`：补登 v34 / v35 / v36 / v37 四条 5.15up 迁移索引。

### 验证

- `tsc --noEmit`、`eslint` 均通过（`.next/types` 陈旧报错为切分支残留，与本次无关）。

### 涉及文件

```
修改：app/api/admin/model-providers/[id]/route.ts
修改：app/api/agents/[id]/greeting/route.ts
修改：app/api/admin/agents/[id]/route.ts
修改：app/admin/agents/page.tsx
修改：supabase/MIGRATIONS.md
```

---

# B 旁支 · 同日 PDF / 图片识别排查与修复（master 分支）

# 5.15up 变更记录

日期：2026-05-15
分支：master（用户授权"时间紧迫，直接在主分支动手"）
主题：上传附件 PDF / 图片识别问题排查与修复

---

## 1. 背景

线上反馈：上传 PDF 提示"文本提取失败，请确认文件未加密"；上传图片智能体识别全部错误。
用户在本地复现：PDF 能正常识别，图片识别全部错误，要求仔细排查。

两条线分别做了**可复现的实证实验**定位真因，结论如下。

---

## 2. 图片识别失败 —— 不是程序 bug，是 Coze 智能体本身无视觉能力

### 排查方法

测试对象 AGTXS-012「知识库入库整理智能体」是 Coze 平台智能体（bot_id `7636677333616918537`）。
用该智能体的 Coze API key 直连 Coze `/v3/chat` API，完全复刻 portal adapter 发出的多模态请求，
两种方式各测一遍：

| 方式 | 说明 | Coze HTTP | 机器人回复 |
|---|---|---|---|
| `object_string` + `file_url` | adapter 当前默认走这条（图片公网链接） | 200，无报错 | "我无法直接访问和读取图片内容" |
| `object_string` + `file_id` | 先上传图片到 Coze 再引用，Coze 原生最优方式 | 200，无报错 | "目前我无法直接读取图片中的文字内容" |

### 结论

- Coze 两次都返回 200、无任何结构性报错 → **portal 发的多模态格式正确，Coze 接收正常**。
- 但这个 bot 自己回复"我的功能是处理您直接提供的文本信息" —— 它用的是纯文本模型，
  未开启图片理解能力。
- 之前那次"识别成《收入证明》模板"是模型在没看到图的情况下**编造**的，所以"全部错误"。
- **portal 端代码无需修改。** 要支持图片识别，需在 Coze 扣子平台把该 bot 的模型
  换成支持视觉的模型 —— 属 Coze 侧 bot 配置，portal 不可控。

---

## 3. 线上 PDF "文本提取失败" —— standalone 构建漏打原生依赖 @napi-rs/canvas

### 根因

- 本地 `next dev` 用完整 `node_modules`，所以本地测 PDF 正常；线上跑 `output: standalone` 构建产物。
- pdf-parse → pdfjs-dist 依赖原生模块 `@napi-rs/canvas`，pdfjs 在 Node 环境靠它
  polyfill `DOMMatrix` / `ImageData` / `Path2D`。
- 检查 `.next/standalone/.../node_modules/` —— **无 `@napi-rs` 目录**。
  `@napi-rs/canvas` 是原生模块 + optionalDependency，Next.js 文件追踪(nft) 静态抓不到；
  而 `next.config.ts` 的 `outputFileTracingIncludes` 里列了 pdf-parse / pdfjs-dist /
  mammoth / xlsx / jszip，**唯独漏了 `@napi-rs`**。

### 实证实验

把本地 `node_modules/@napi-rs` 临时移走（模拟线上 standalone 缺该模块），再跑 PDF 提取：

```
Warning: Cannot load "@napi-rs/canvas" package
[getText THREW] ReferenceError: DOMMatrix is not defined
```

实验后已还原。这就是线上 PDF 失败的真因。老代码 `catch {}` 把该异常吞掉，
统一误报"文本提取失败，请确认文件未加密" —— 与加密毫无关系。

---

## 4. 修复

### 4.1 next.config.ts —— 把 @napi-rs 纳入 standalone 打包

`outputFileTracingIncludes` 的 `/api/upload` 和 `/api/trial/chat` 两处各加一行：

```
"./node_modules/@napi-rs/**/*",
```

`/**/*` 覆盖 `@napi-rs/canvas`、平台原生二进制子包（`canvas-linux-x64-gnu` 等）、
`wasm-runtime`，全部打进 standalone。

### 4.2 app/api/upload/route.ts —— 异常分类 + 日志

- PDF catch：原 `catch {}` 改为带异常分类。
  - `PasswordException` / 含 password|encrypt → "文件已加密，请上传未加密版本"
  - `InvalidPDFException` / 含 invalid pdf → "文件已损坏或不是有效 PDF"
  - 其它 → "文本提取失败 · {errName}: {errMsg}"（诊断期把真实异常带到前端，定位后可收敛文案）
  - 全部分支加 `console.error` 日志
- PDF 提取成功但内容为空 → 提示"未提取到文字（可能是扫描件 / 图片型 PDF）"
- docx / xlsx / pptx 三处 `catch {}` 补 `console.error` 日志（pdf/docx 之前已有，本次补 xlsx/pptx）

---

## 5. 部署前提（重要，需确认）

`@napi-rs/canvas` 是原生模块，二进制按平台分包：

- 本地这台是 Windows，`node_modules/@napi-rs/` 只有 `canvas-win32-x64-msvc`
- 线上 Linux 需要 `canvas-linux-x64-gnu`

本修复成立的前提是 **Jenkins 构建在 Linux 服务器上跑 `npm install` / `npm ci`**，
这样会自动装上 Linux 版原生二进制，`@napi-rs/**/*` 这个 glob 才能把它打进 standalone。
若构建流程是把 Windows 上构建好的 `node_modules` 整包上传，则本修复不足以解决问题。

---

## 6. 验证

- 本地已用"移走 @napi-rs"实验反证根因，无需再跑。
- 真正验证点在线上：本次构建部署后上传 PDF 应能正常提取文字。
- 万一仍失败，新代码的异常分类会把真实 `errName: errMsg` 直接带到前端 + 写入
  `console.error` 日志，可一眼定位。

---

## 7. 涉及文件

```
修改：next.config.ts（outputFileTracingIncludes 两处加 @napi-rs）
修改：app/api/upload/route.ts（PDF 异常分类 + xlsx/pptx catch 加日志）
新建：upgrade/5.15up/变更记录-20260515.md（本文件）
```

---

## 8. 待办

- 确认 Jenkins 构建是否在 Linux 上做依赖安装（见第 5 节）。
- 部署后线上回归：PDF / docx / xlsx / pptx / 图片上传全链路。
- 图片识别：如需支持，在 Coze 扣子平台把对应 bot 换成视觉模型。
- 诊断期文案收敛：PDF 其它异常分支当前把原始 `errName: errMsg` 暴露给前端，
  线上定位完成后可改回友好文案。

---

## 9. 对话气泡横向溢出修复

### 问题

智能体对话页消息列表底部出现横向滚动条，用户无法在窗口内一次看全对话内容。

### 根因

气泡列 `max-w-[75%]` 是 flex 子项但缺 `min-w-0`，默认 `min-width: auto` 不小于内容
最小宽度。当气泡内出现"不换行的元素"时会突破 75% 限制把整行顶出滚动容器：

- Markdown 表格（`.trial-md table` 没做限宽，宽表格直接撑破气泡）
- 用户消息里的超长无空格字符串（用户气泡只有 `pre-wrap`，不断长串）

### 修复

- `app/agents/[id]/page.tsx`
  - 气泡列：`max-w-[75%]` 加 `min-w-0` —— 允许收缩，超宽内容改为内部滚动而非撑破
  - 气泡本体：加 `min-w-0 break-words` —— 用户消息长链接也能断行
  - 消息滚动容器：加 `overflow-x-hidden` —— 兜底，杜绝任何意外溢出再产生整页横向滚动条
- `app/globals.css`
  - `.trial-md table`：加 `display:block; max-width:100%; overflow-x:auto` ——
    宽表格在气泡内自己横向滚动

效果：气泡严格不超过 75% 宽，整页不再出现横向滚动条。

### 涉及文件

```
修改：app/agents/[id]/page.tsx（气泡 min-w-0 / break-words，滚动容器 overflow-x-hidden）
修改：app/globals.css（.trial-md table 限宽滚动）
```

### 补充修复：代码块仍顶出气泡 + 短消息不该长气泡

气泡外框限住后，气泡**内部**的 ```代码块``` 长行仍顶出。中途一版用"气泡定宽
撑满列"解决，但导致短回复也变成长气泡，用户不接受。最终方案两者兼顾。

**根因**：assistant 气泡是 flex 子项 + `items-start` → 按内容 shrink-to-fit。
代码块 `<pre>` 是 `white-space: pre`（不换行），最小内容宽度 = 最长那一行；
非 stretch 的 flex 子项不会被自动夹到容器宽度，`<pre>` 拿不到定宽，
`.trial-md pre` 的 `overflow-x:auto` 不触发。

**最终修复**（`app/agents/[id]/page.tsx`）：

- 气泡列：`w-full min-w-0` 撑满整行，只作气泡 `max-w` 百分比的**定宽参照**（自身不可见）
- 气泡本体：`max-w` 给在气泡上（assistant **97%** / user 75%），
  按 `isAssistant` 取值；气泡保持 shrink-to-fit —— **短消息=小气泡**，
  长内容/代码块涨到上限封顶
- 气泡 `min-w-0`：让 `max-w` 能压过 `<pre>` 的 `min-content`，
  超宽时 `<pre>` / 表格在气泡内 `overflow-x` 滚动而非顶出

效果：短回复 = 小气泡；长回复 / 代码块 = 最多 97%，代码块、宽表格在气泡
**内部**横向滚动不顶出；user 气泡保持 ≤75% + 右对齐。

> assistant 上限调整过程：75% → 90% → 100% → 95% → 最终定 **97%**（用户拍板）。

---

## 10. 智能体开场白支持全平台

### 问题

只有 Coze 智能体进对话页有开场白；智谱清言 / 腾讯元器 / Dify / OpenAI 等没有。

### 根因

`app/api/agents/[id]/greeting/route.ts` 原本只对 Coze 调 `get_online_info` 拉
后台配的 prologue。其它平台**没有"读取智能体开场白"的公开 API** —— 不是没对接，
是平台方没这个接口，无法自动拉取。

### 修复

greeting 路由改为：**优先返回平台侧自己存的开场白**，任意平台通用 ——

- 读 `agent.builder_config.opening_message` 或 `agent.model_params.opening_message`
  （前者优先，兼容 5.15up 搭建器；后者是手动配置的智能体走的字段）
- 命中则返回 `{ prologue, suggested_questions }`，不分平台
- 没有平台侧开场白时，仅 Coze 仍走原 `get_online_info` 自动拉取兜底

### 用法

管理员在「智能体管理 → 某智能体 → API 配置 → 模型参数 JSON」里加一个
`opening_message` 字段即可（可选 `suggested_questions` 数组）：

```json
{ "...其它参数...": "...", "opening_message": "你好，我是…" }
```

不用建表（`builder_config` 列 migration_v36 已有；`model_params` 本就存在），
不用新增 UI —— 复用现有的「模型参数 JSON」输入框。

### 涉及文件

```
修改：app/api/agents/[id]/greeting/route.ts
```
