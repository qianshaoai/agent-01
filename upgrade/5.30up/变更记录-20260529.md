### 5.30up 变更记录

日期：2026-05-29
分支：master2
依据：[方案-org_admin-API+KB管理权限-20260529.md](./方案-org_admin-API+KB管理权限-20260529.md)（R2 通过）

---

## Phase 0 · 共用基建（已完成）

A / B 两半的共用前置工作，必须先做。本 commit 不动业务路由，仅落 DB schema +
工具库 + 审计基础设施。

### 改动

1. **新增 [`supabase/migration_v43_scoped_ownership.sql`](../../supabase/migration_v43_scoped_ownership.sql)**
   - `model_providers` + `knowledge_bases` 各加 `tenant_code TEXT NULL` 列 + 单列 BTREE 索引
   - 存量数据默认 NULL（= 平台公共），与现有行为完全兼容
   - 不加 FK 到 `tenants(code)`（项目惯例软引用 + 应用层校验）
   - RLS 仍 DISABLE（与现状一致）
   - 全部 `IF NOT EXISTS`，可重跑

2. **同步更新 [`supabase/MIGRATIONS.md`](../../supabase/MIGRATIONS.md)** 加 v43 索引条目
   - 标注【🔑 RBAC 必跑】+ 5.30up 后端依赖说明（不跑就 5xx）

3. **新增 [`lib/scoped-access.ts`](../../lib/scoped-access.ts)** —— A/B 半共用 ownership 工具库
   - 5 个核心同步 helper：`listScopeFilter` / `canReadRow` / `canWriteRow` /
     `resolveCreateOwnership` / `sanitizeUpdatePatch`
   - R2 §6 · `validateTenantCode(code)` async helper：查 `tenants.code` 存在
   - R2 §4 · `scanReferences(kind, id)` async helper：扫资源引用面（含 draft JSON `.contains()` 查询）
   - R2 §1 · `requireWriteAccess(admin, allowedRoles)` 路由层双闸总闸门
   - `ScopeAdminNoTenantError` 自定义错误类（防 org_admin 缺 tenantCode 走兜底）
   - 所有 helper 对 `admin.role === "org_admin" && !admin.tenantCode` **全部 fail-closed**
   - `listScopeFilter` 用 OR filter string 返回，避免泛型 QueryBuilder 依赖

4. **改 [`lib/audit.ts`](../../lib/audit.ts)** —— R1 §5 审计基础设施补全
   - `AuditResourceType` 加 `knowledge_base`
   - `resolveResourceTenantCode` 加 `model_provider` / `knowledge_base` 两个 case，从对应表读 tenant_code

### 单测说明（与方案承诺的偏差）

方案 R2 写"33 个 scoped-access 单测"，**实施时发现项目无测试框架**（`ci:test` 是 placeholder
`Tests pending (see M2.2 in 4.17up)`）。决策：

- **不擅自引入新依赖**（vitest / jest 等），避免范围扩展
- 单测口径**改为 Phase Z 联合验收期的 e2e 渗透脚本**（curl + 真实 dev DB），覆盖方案的 28 条
  渗透用例 + 各半专有用例
- 引入测试框架是独立议题，待小B / 用户评审决定，不在本期 5.30up 范围

### 验证

- [x] `npm run ci:typecheck` 通过
- [x] `npm run ci:lint` 通过（仅 pre-existing warning · agents/[id]/page.tsx:761）
- [ ] 上线时跑 `migration_v43`（dev DB 先；线上 DB 后）
- [ ] 跑完同步打 ✅ 到 [`MIGRATIONS.md`](../../supabase/MIGRATIONS.md)

### 变更文件

- 新增 [`supabase/migration_v43_scoped_ownership.sql`](../../supabase/migration_v43_scoped_ownership.sql)
- 改   [`supabase/MIGRATIONS.md`](../../supabase/MIGRATIONS.md) — 加 v43 索引条目
- 新增 [`lib/scoped-access.ts`](../../lib/scoped-access.ts) — 5 核心 + validateTenantCode + scanReferences + requireWriteAccess
- 改   [`lib/audit.ts`](../../lib/audit.ts) — 加 `knowledge_base` 类型 + 两个 resolveResourceTenantCode 分支

---

## 下一步（A 半）

- Phase A.1 · model-providers 6 路由改造（list + CRUD + test）
- Phase A.2 · agent-drafts 5 条链路补 canReadRow（POST / PATCH / duplicate / test-chat / publish）
- Phase A.3 · 前端 model-providers 页 + 弹窗 + admin-layout 导航

B 半（KB 管理）按方案与 A 并行，由独立任务推进。
