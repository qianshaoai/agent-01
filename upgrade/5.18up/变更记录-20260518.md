# 5.16up 变更记录

日期：2026-05-18
分支：feature/agent-builder-pr-d
主题：GPT 接入干净重做（W1 token 统计 + W2 加权扣额度）

方案见 [GPT接入与摘要降本-干净重做方案-20260518.md](./GPT接入与摘要降本-干净重做方案-20260518.md)。
不 merge devA，按 pr-d 当前架构把 devA 5.7up GPT 接入的增强重做。本期做 PR-1 + PR-2，
W3（会话摘要降本）后续另开分支。

---

## PR-1 · 补 v25 / v26 迁移文件入仓库

devA 的 `migration_v25.sql` / `migration_v26.sql` 这两个迁移**数据库已跑过**，但
**文件只在 devA 分支上**，pr-d 仓库里没有 —— 别人按 `MIGRATIONS.md` 重建库会缺步骤。
PR-1 只补仓库完整性，**0 运行风险，不重跑 SQL**。

### 改动

- 从 devA 分支拷入 `supabase/migration_v25.sql`：
  - `tenants` 加 `openai_key_enc / openai_key_set_at / openai_key_set_by`
  - `logs` 加 `prompt_tokens / completion_tokens / model_used` + `logs_model_used_idx` 索引
  - 新建 `model_quota_weights` 表（种子 4o-mini=1 / 4o=5 / o1 系列默认禁用）
  - 新增 RPC `increment_quota_used_weighted(p_code, p_weight)` —— 内部守卫
    `quota_used + weight <= quota` 才扣（W2 的"带权重预检查、不超扣"正是对齐它）
- 从 devA 分支拷入 `supabase/migration_v26.sql`：
  - `conversations` 加 `summary_text / summary_until_at`（W3 用，本期不动）
- `supabase/MIGRATIONS.md`：补登 v25 / v26 两行（标注用户已跑、文件 5.16up 补录）

### 涉及文件

```
新建：supabase/migration_v25.sql（从 devA 拷入）
新建：supabase/migration_v26.sql（从 devA 拷入）
修改：supabase/MIGRATIONS.md
新建：upgrade/5.16up/变更记录-20260518.md（本文件）
新建：upgrade/5.16up/GPT接入与摘要降本-干净重做方案-20260518.md（已先行提交）
```

---

## PR-2 · W1 token 统计 + W2 加权扣额度

### W1 · Token 用量统计

- `lib/adapters/index.ts`：
  - `AdapterConfig` 加 `onUsage?` 回调 + 导出 `TokenUsage` 类型。
  - openai 兼容适配器：**仅 `platform === "openai"`** 时请求带
    `stream_options: { include_usage: true }`（小B finding 3 —— zhipu 等兼容平台
    不认会 400）；SSE 末 chunk 带 `usage` 时回调 `onUsage`。
- `app/api/agents/[id]/chat/route.ts`：`streamChat` 传 `onUsage` 捕获
  `capturedUsage`；流结束写 `logs` 带 `model_used`（模型已知就记）+
  `prompt_tokens / completion_tokens`（有 usage 才记）。
- `app/admin/logs/page.tsx`：日志表加「模型」「Token(in/out)」两列展示
  （小B finding 5 用户选乙）。

### W2 · 按模型加权扣额度

`app/api/agents/[id]/chat/route.ts`：

- **解析顺序重排（小B 复评）**：把"provider/key 解析 + 算模型 + 算 weight"
  整体从原步骤 6 提到**步骤 2**，在配额检查、建会话、写用户消息**之前**完成。
- provider 查询 `select` 补 `default_model`（小B finding 2）。
- 当前模型 `modelUsed`：`agent.model_params.model || provider.default_model ||
  （仅 openai）"gpt-4o-mini"`，与适配器内部兜底对齐；非空时回填
  `resolvedModelParams.model`，保证 日志/扣费/真实调用 三者一致。
- 算 `weight`：查 `model_quota_weights`，命中且 `enabled` → `weight_per_call`；
  查不到 / 未 enabled → `weight = 1`（决策 3/4，模型驱动、软放过）。
- **带权重配额预检查（小B finding 1，方案甲）**：
  `quota_used + weight > quota` 即拦，与 `increment_quota_used_weighted` 内部
  守卫口径一致 —— 不漏扣、不超扣。
- 扣费：`weight > 1` 走 `increment_quota_used_weighted`，否则原
  `increment_quota_used`；**移出 `Promise.all`**，检查 `{ data, error }`，
  `error` 或加权 RPC 返回 `false`（余额不足/并发）→ `console.error` 告警（小B 复评）。

### 验证

- `tsc --noEmit` + `eslint` 通过。
- 待本地回归（重点 —— 见方案第八节）：
  - openai 智能体对话 → `logs` 有 token / model；日志页显示
  - **账务边界**：余额 1 次、调权重 5 的模型 → 预检查拦下，不白嫖
  - 非 openai（Coze/清言）智能体 → 仍按次扣 1、不退化
  - 个人用户不扣、aborted 不扣

### 涉及文件

```
修改：lib/adapters/index.ts
修改：app/api/agents/[id]/chat/route.ts
修改：app/admin/logs/page.tsx
```

### 小B PR-2 评审修复

小B 评审 PR-2 提了 2 点，均已补（`app/api/agents/[id]/chat/route.ts`）：

- **P2 · weight=1 扣费失败漏检**：原来 `ded.data === false` 只在 `weight > 1`
  时查。`increment_quota_used` 也返回 boolean，并发下 weight=1 的请求也可能
  返回 false（没扣成）。改为**两种 RPC 都查 `data === false`**，没扣成即告警。
- **P3 · 权重表查询 error 被当 weight=1**：原来 `model_quota_weights` 查询只看
  `data` 不看 `error`，迁移漏跑 / 表异常 / DB 故障时贵模型会静默按 1 扣。
  改为查询出错即 `console.error` + **返回 503 阻断**（与"配额检查 DB 故障即 503"
  同口径，不静默少扣）。

小B 验证：`ci:typecheck` / `ci:lint` / `ci:build` 均通过；PR-1 的 v25/v26 文件
与 MIGRATIONS.md 登记也在。

**小B 复验（5/18）：通过。** P2/P3 已确认修到，未发现新的阻断问题。

> **已知非阻断残余风险**：当前是"先调模型、后扣费"架构 —— 并发下扣费 RPC 返回
> `false`（没扣成）时，回答已发给用户，只能打错误日志事后追，无法回滚这次免费回答。
> 要彻底消除需改成"调用前预占 / 预扣额度"。本期按方案口径不做，登记为后续可选改进。

### 后续

- W3（会话摘要降本）—— 本期不做，W1+W2 上线确认无误后另开分支。
- 可选改进：quota 改"调用前预占"，消除并发下"先调后扣"的免费回答残余风险。

---

## PR-2 补丁 · 剩余次数计数器识别 weight

### 问题

W2 已完成后端加权扣费与后台日志展示，但用户对话页顶部的"剩余次数"计数器仍固定
每次 `left - 1`。加权模型（如 `gpt-4o` 实扣 5）发完后，页面只少 1 次，刷新后才恢复
真实余额，容易让员工误以为额度还很多。

### 修复

- `app/api/agents/[id]/chat/route.ts`：SSE `done` 事件带上本次 `weight`：
  `{ done: true, conversationId, weight }`。
- `app/agents/[id]/page.tsx`：收到 `done` 后按 `obj.weight ?? 1` 乐观扣减剩余次数。
  服务端未带 `weight` 时仍按 1 扣，兼容旧行为。

### 不涉及

- `user-agents` / `trial` 路由没有加权扣费，保持原行为。
- 扣费 RPC 失败导致的乐观扣减漂移仍属"先调模型、后扣费"架构残余风险，刷新即可恢复；
  彻底解决需后续改成调用前预占 / 或 route 直接返回真实剩余额度。

### 涉及文件

```
修改：app/api/agents/[id]/chat/route.ts
修改：app/agents/[id]/page.tsx
```

### 验证

- `tsc --noEmit`（ci:typecheck）+ `eslint`（ci:lint）通过。
- 待本地回归：
  - gpt-4o 智能体（weight 5）发一句 → 顶部「剩余次数」减 5
  - gpt-4o-mini / coze 智能体发一句 → 减 1
  - 刷新页面前后计数一致（与后台 `quota_used` 对得上）

---

## Fix 3 收口 · 加密 key 拆分（存量 rekey + 部署脚本收口）

方案见 [加密key拆分收口-rekey方案-20260518.md](./加密key拆分收口-rekey方案-20260518.md)（v3）。

### 背景

5.14up 已实现「路径 A · Fallback Chain」（`lib/crypto.ts`），但只「向前修复」：上线前
已入库的存量密文仍用 `sha256(JWT_SECRET)` 旧 key 加密。用户决策（5/18）：补一次性
rekey，把存量密文整体重写为新 key `ENCRYPTION_KEY`，彻底关闭「JWT_SECRET 泄漏 = 全
平台密钥沦陷」。

### 改动

- **新建 `scripts/rekey-encryption.mjs`**：把 `agents` / `model_providers` /
  `user_agents` 的 `api_key_enc` + `tenants` 的 `openai_key_enc`（4 表，主键 `id`）
  逐行从旧 key 重写为新 key。自带加解密不 import crypto.ts；新 key 只从 env 读不进
  argv；启动校验 base64 32 字节 + 新旧 key 不相等（防伪拆分）；逐行五分类
  `empty/new_hit/old_hit/plaintext_shape/failed`；写前回环自校验；问题行退出码非 0；
  dry-run 默认、`--apply` 实写。
- **`scripts/migrate-trial-agents.mjs`**（阻断点收口）：该脚本挂在 Jenkins 部署
  流水线、每次部署重写 3 个 trial agent 的 `api_key_enc`。原内联加密用旧 key —— rekey
  后会被它反向污染。已改为用 `ENCRYPTION_KEY` 新 key（`getKey()` base64 解码 32 字节、
  不 sha256）；`REQUIRED_ENVS` 用 `ENCRYPTION_KEY` 替换 `JWT_SECRET`；移除多余的
  `createHash` import。
- **`scripts/migrate-agent-keys-to-providers.mjs`**：一次性脚本、不反向污染，但
  `decrypt()` 原只认旧 key。已补「新 key 优先、回退旧 key」fallback；头部加废弃注记。
- **`lib/crypto.ts`**：订正陈旧注释（原写「production 不允许 fallback，进程启动期
  直接抛错」，实际无启动校验、`decrypt()` 静默回退、`encrypt()` 才抛）。**无逻辑改动。**
- **`Jenkinsfile`**：trial stage 注释由「用线上 JWT_SECRET」同步改为 `ENCRYPTION_KEY`。
  **无逻辑改动。**

### 小B 两轮评审

- 首轮 5 点（→ 方案 v2）：补 trial 脚本收口（阻断）、key 不进 argv、新旧 key 分类
  计数、非三段格式当问题、修「启动即抛错」表述。
- 复评 2 收口点 + 2 小建议（→ 方案 v3）：rekey 脚本校验新旧 key 不相等、
  `migrate-agent-keys` 补 fallback decrypt + 废弃注记、修 crypto.ts 与 Jenkinsfile
  陈旧注释。均已落地。

### 验证

- `node --check` + `eslint` 三个 `.mjs` 脚本均通过。
- rekey 实跑在上线部署后按方案 §五 步骤 [4][5] 进行（dry-run → `--apply` → 再
  dry-run 验 `old_hit=0`）。

### 小B 复验补充（方案 v3 复验）

小B 复验提 3 点，均已落地：

- **CAS 写库**：`rekey-encryption.mjs` 写库改成条件更新 `.eq(col, 读取时密文)` ——
  防 rekey 读取后管理员并发改 key 被旧值覆盖；不命中即计 `concurrent_changed`、
  不覆盖，重跑确认。
- **运行位置**：部署目录只有 standalone 产物、无 `scripts/`，无法在部署目录跑。
  新增 `Jenkinsfile.rekey` —— 手动触发的一次性 job（checkout + npm ci + 凭据注入 +
  跑脚本，`APPLY` 参数控制 dry-run / 实写）。
- **分页稳定排序**：`fetchAll` 加 `.order("id")`，防分页过程漏扫 / 重复扫。

### 涉及文件

```
新建：scripts/rekey-encryption.mjs
新建：Jenkinsfile.rekey
修改：scripts/migrate-trial-agents.mjs
修改：scripts/migrate-agent-keys-to-providers.mjs
修改：lib/crypto.ts（仅注释）
修改：Jenkinsfile（仅注释）
新建：upgrade/5.16up/加密key拆分收口-rekey方案-20260518.md
```

### 后续（操作类，待上线时做）

- 生成 `ENCRYPTION_KEY`（`node -e "..."`）→ 建 Jenkins 凭据 `agent01_ENCRYPTION_KEY`
  → 密码管理器留存。**key 不经过任何对话/聊天。**
- 上线时序：建凭据 → 备份 DB → 合并部署 → 部署后跑 rekey → 验证。

---

## 5.16up 产品需求 · 方案 A（快速交付包）

方案见 [产品需求6项-方案A-快速交付包-20260518.md](./产品需求6项-方案A-快速交付包-20260518.md)，
统一约束见 [产品需求6项-并行开发统一约束-20260518.md](./产品需求6项-并行开发统一约束-20260518.md)。
本段为方案 A（R2 + R3 + R1）变更，方案 A 不碰 `app/admin/workflows/page.tsx` 及方案 B 文件范围。

### PR-A1 · R2 注册页单卡片视觉同步登录页

需求按 5/18 调整执行：注册页**不做 showcase 图、不做双栏**，保留单卡片、UI 视觉同步登录页。

- `app/register/page.tsx`：
  - 背景换成登录页同款径向深蓝渐变；顶栏（logo 容器、平台名）样式对齐登录页。
  - 注册卡片改成深蓝渐变玻璃卡片（`from-[#001f7a]/85 …`）；按前端约束**不加光晕 / blob 装饰**，仅保留顶部高光线与柔光叠层。
  - 小B 验收（5/18）：注册页两个模糊圆形光晕装饰违反前端约束，已删除。
  - 输入框、按钮、标签、错误条、链接全部改深蓝底配色（白字、半透明输入框）。
  - 「个人 / 组织」切换改成深蓝底玻璃态分段控件。
  - 底部补一条「已有账号？去登录」链接，与登录页底部「立即注册」对称。
- **不改**注册接口 / 字段 / 校验逻辑；`siteSettings` 仍只取 `logo_url / platform_name`，
  未引入 `login_showcase_url`。

### PR-A2 · R3 已放弃工作流可恢复

- `app/api/workflow-sessions/[id]/route.ts`（后端）：
  - `PATCH` 加**状态流转白名单**，只约束带 `status` 的请求：
    `abandoned→in_progress`（恢复）/ `in_progress→completed` / `in_progress→abandoned` 放行；
    同状态幂等放行；其余流转 422；非法 status 值 422。
  - 带 `status` 时先按 `id + user_id` 取当前状态校验来源，顺带校验会话归属。
  - 不带 `status` 的纯改名 / 改进度请求**不受影响**，逻辑不变。
- `app/workflows/history/page.tsx`（前端）：
  - `abandoned` 行新增「恢复」按钮（`completed` 行不显示），调 `PATCH {status:"in_progress"}`。
  - 恢复成功后该行从历史列表移除，alert 提示回首页「进行中」继续。
  - `current_step_idx` 由后端保留，从放弃时的进度继续。

### PR-A3 · R1 智能体搭建器体验打磨

范围锁定为 3 项（视觉 / 错误提示 / 帮助），不动数据库、不动业务逻辑。

- `app/admin/agent-builder/page.tsx`（草稿列表页）：
  - 自制 `flash()` 提示条 → 全局 `useToast()`；移除 `msg` state 与提示条 DOM。
- `app/admin/agent-builder/[id]/page.tsx`（编辑页）：
  - 同样把 `flash()` → `useToast()`、移除自制提示条（与列表页一致，对应方案 D1-2「保持全站一致」）。
  - 5 个分区标题各补一句说明文字（`SectionTitle` 加可选 `desc` 属性）。
  - 顶部新增「使用说明」按钮，点开折叠面板讲解 配置 → 保存 → 测试 → 发布 流程。
  - 测试聊天错误文案优化：空响应、HTTP 失败给可操作建议，不暴露内部错误。

### 验证

- `npx tsc --noEmit`（等价 `ci:typecheck`）通过，无报错。
- `npx eslint` 跑 5 个改动文件，通过，无 error / warning。
- 约束文档建议的 `npm run typecheck` / `npm run diff-check` 在 `package.json` 里无对应
  脚本，实际用 `ci:typecheck` / `eslint` 等价替代。
- **手工回归待人工**（本环境无法跑浏览器）：
  - 注册页：个人注册、组织注册、字段校验、窄屏卡片滚动；
  - 历史页：abandoned 行可恢复、completed 行不显示恢复、永久删除不受影响、`completed→in_progress` 被拒；
  - 搭建器：列表 / 编辑页 toast、使用说明面板、5 分区说明、测试聊天与发布。

### 涉及文件

```
修改：app/register/page.tsx
修改：app/api/workflow-sessions/[id]/route.ts
修改：app/workflows/history/page.tsx
修改：app/admin/agent-builder/page.tsx
修改：app/admin/agent-builder/[id]/page.tsx
```

---

## 5.16up 产品需求 · 方案 B（后台管理增强包）

方案见 [产品需求6项-方案B-后台管理增强包-20260518.md](./产品需求6项-方案B-后台管理增强包-20260518.md)，
统一约束见 [产品需求6项-并行开发统一约束-20260518.md](./产品需求6项-并行开发统一约束-20260518.md)。
分支 `feature/5.16up-plan-b`（从 pr-d 拉）。本段仅方案 B（R4/R5/R6）变更，不碰方案 A 文件范围。

### PR-B0 · 产品拍板记录

写代码前先完成产品拍板（约束硬门槛），结果写进方案 B 文档第二节：

- D4-1 多分类智能体归位：**每个所属分类下都出现**
- D4-3 智能体展示：**完整展示（全量分组）**
- D6-1 工作流排序：**方案乙·仅分区展示（不改 DB）**
- D5-1 步骤拖拽估时：**约 1 天**

### PR-B1 · R4 智能体分类展示

只改 `app/admin/agents/page.tsx`（方案 B 范围内，后端 `route.ts` 未动）：

- **修筛选漏项**：分类筛选由遗留单列 `a.category_id` 改为多分类 `a.categoryIds.includes()`，
  多分类智能体不再被漏筛。
- **完整展示**：`load()` 改为 `fetchAllAgents()` 分页循环拉完所有智能体（每页 100，
  约束允许的前端分页循环法，不动后端）。
- **按分类分组**：列表由扁平表改为分类分区 —— 每个分类一个 `<tbody>` 区块（图标+名称+
  数量），多分类智能体在每个所属分类区块各出现一次（D4-1）；空 `categoryIds` 归入末尾
  「未分类」兜底区块；分区顺序按 `categories`（接口已 `order(sort_order)`）；分类筛选
  激活时只展示该分区。
- 移除随之失效的 `urlPageSize`（列表已全量加载，`?pageSize` 参数不再需要；`?focus`
  跨页定位仍保留，且因全量加载而更稳）。

**小B 验收修复**：多分类智能体在多分区有重复行，「+N 更多」弹层状态 `openMoreFor` 原只用
`a.id` 做 key，会令同一智能体所有重复行的弹层一起打开 —— 改为 composite key
`${section.id}:${a.id}`。

### 验证

- `npm run ci:typecheck` / `npm run ci:lint` 通过；小B 验收 `npm run build` 亦通过。
- 手工回归待人工：分类分区展示、多分类智能体各分区均出现、未分类兜底、分类/类型/状态
  筛选、`?focus` 定位高亮、「+N 更多」弹层只开当前行、org_admin 只读按钮不丢。

### 涉及文件

```
修改：app/admin/agents/page.tsx
```

### PR-B2 · R5 工作流步骤拖拽改顺序

落库按约束 §四.7 走原子 RPC 优先。

- `supabase/rpc.sql`：新增 `reorder_workflow_steps(p_workflow_id, p_step_ids)` ——
  plpgsql 函数体即单事务：校验「传入 id 集合 === 该工作流当前步骤集合」（数量 + 每个
  id 真属该工作流），通过则按数组下标原子重设 `step_order`，要么全成要么全不动。
  **⚠ 需在 Supabase SQL Editor 跑一次。**
- `app/api/admin/workflows/[id]/steps/route.ts`：新增 `PUT` —— 接收完整有序
  `stepIds` 数组；沿用 POST 同口径的 `canActOnRole` 上下级权限校验；校验非空 / 无重复；
  调 RPC 重排；RPC 返回 false（集合不一致）→ 422，前端须刷新。
- `app/admin/workflows/page.tsx`（仅步骤**列表视图**，不动流程图视图）：
  - `GripVertical` 抓手变真实拖拽入口（HTML5 DnD：抓手为拖动源、步骤行为放置区）。
  - 拖放结束调 `PUT .../steps` 提交完整有序数组；乐观更新，失败 `toast` + 强制
    `load()` 重拉真实顺序、不保留错误乐观序。
  - 列表视图步骤行补「上移 / 下移」按钮（复用既有 `moveStep`），作窄屏 / 无障碍 fallback。
  - 沿用 `canTouchWf` 权限：无权工作流抓手禁用、按钮置灰。

### PR-B3 · R6 工作流排序优化（方案乙）

按 D6-1 拍板走**方案乙·仅分区展示**，不改 DB。

- `app/admin/workflows/page.tsx`：工作流卡片列表由扁平列表改为**按分类分区**展示 ——
  每个分类一个分区（图标 + 名称 + 数量），多分类工作流在每个所属分类分区各出现一次
  （与 R4 后台分组口径一致），空分类归末尾「未分类」兜底区；分类筛选激活时只展示该分区。
  区内仍复用全局 `workflows.sort_order`、分页限制仍在。
- 列表顶部加说明「分区视图，非各分类独立排序」—— 不宣称真隔离。

### 验证

- `npm run ci:typecheck` / `ci:lint` / `ci:build` 均通过。
- 手工回归待人工：步骤拖拽（含跨权限、空 / 单步骤工作流）、上下移 fallback、拖拽失败
  回真实序、工作流分区展示、多分类工作流各区出现、未分类兜底、`?focus` 定位。

### 涉及文件

```
新增 PUT：app/api/admin/workflows/[id]/steps/route.ts
新增 RPC：supabase/rpc.sql（reorder_workflow_steps，需跑 SQL）
修改：app/admin/workflows/page.tsx（R5 拖拽 + R6 分区）
```

### 小B 验收修复（方案B）

小B 验收方案 B 提 1 个阻断点，已补：

- **R5 PUT 漏 org_admin 归属校验**：新增的 `PUT /workflows/[id]/steps` 只做了
  `created_by_role` 上下级校验，没校验 org_admin 对该工作流是否有归属权 —— 组织管理员
  理论上猜到同级 org_admin 建的 workflow id，就能重排别组织的步骤。已补
  `ensureOrgAdminCanTouch`（与 `workflows/[id]` 的 PATCH/DELETE 同口径，按本仓库既有
  写法在 route 文件内联）。

- **顺手收口历史 POST 口子**：同文件的 `POST`（新建步骤）也补同款
  `ensureOrgAdminCanTouch`，避免 org_admin 往非归属工作流里新增步骤。该问题不是 R5
  引入，但 helper 已在本文件内，顺手补齐同一类写操作边界。

### 方案 B 完成情况

PR-B0 + PR-B1（R4）+ PR-B2（R5）+ PR-B3（R6）全部完成；小B 验收阻断点已补 ——
完整方案 B 实现完成。

---

## 回归修复（2026-05-19，上线前）

上线前回归测试发现的问题，逐条修。

### RF-1 · 智能体搭建器供应商下拉混入智能体平台 API

- 现象：搭建器（`app/admin/agent-builder/[id]/page.tsx`）的「模型供应商」下拉里，
  把 coze / 元器 / 清言这些**智能体平台 API**（`model_providers.category=agent`）
  也列了进来。
- 原因：line 140 拉供应商 `fetch("/api/admin/model-providers")` **没带
  `?category=model`** 过滤 —— 把所有供应商全拉进来了。搭建器只建「模型对话型
  智能体」，只该选模型类 API（`category=model`，如 OpenAI / 智谱）。智能体管理页
  的「API 配置」弹窗本来就按 `?category=` 过滤，搭建器这里漏了。
- 修复：line 140 改为 `fetch("/api/admin/model-providers?category=model")`。
  改完下拉只剩模型类 API。`ci:typecheck` / `ci:lint` 通过。

### RF-2 · 发布智能体即启用（去掉 enabled=false 边界）

- 背景：搭建器发布的智能体原默认 `enabled=false`，等 PR-D 聊天链路兼容上线后再
  统一启用。现 PR-D 已就绪，该边界不再需要。
- 改动：
  - `app/api/admin/agent-drafts/[id]/publish/route.ts`：发布 upsert 的 `enabled`
    由 `false` 改 `true`（首发 / 重发布同口径）；审计 detail、响应体同步；删 PR-D
    相关注释与 `note`。
  - `app/admin/agent-builder/[id]/page.tsx`：删两处「发布后默认禁用」黄色横幅
    （发布设置区 + 发布弹窗），发布结果提示改为「已发布并启用」，发布按钮
    `title`、使用说明文案同步。
- `ci:typecheck` / `ci:lint` 通过。

### RF-3 · 搭建器草稿自动保存

- 背景：搭建器原来每次都要手动点「保存草稿」，多一步操作。
- 改动（`app/admin/agent-builder/[id]/page.tsx`）：
  - 草稿内容变化后**防抖 1.5s 自动落库**（`useEffect` 监听 `draft`/`dirty`，
    调 `save({auto:true})` 静默保存）。
  - 保存失败则停下、不循环重试刷 toast，待下次编辑或手动点状态条重试
    （`saveError` 态把自动保存 effect 挡住）。
  - 「保存草稿」按钮换成**保存状态条**（已保存 / 待自动保存… / 保存中… /
    保存失败·点击重试），点击可立即保存。
  - 测试聊天、发布前若有未落库改动，自动 `await save()` 刷盘 —— 不再弹
    「请先保存草稿」拦截。
- `ci:typecheck` / `ci:lint` 通过。
