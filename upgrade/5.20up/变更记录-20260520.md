### 5.20up 变更记录

日期：2026-05-20
分支：`master2`（即 master + devC 合并后的上线候选）
依据：[5.20up 回归风险修复方案](./回归风险修复方案-20260520.md)

---

## P0 · standalone 发布静态资源补拷（落地）

### 现象（方案 §二）

`next.config.ts` 用了 `output: "standalone"`；Next.js 16 不会把 `.next/static` 和 `public`
拷进 standalone 产物，直接跑 `node .next/standalone/agent-01/server.js`：

- `/api/health` → 200（误判"服务没问题"）
- `/_next/static/...` → 404
- `/favicon.ico` 等 public 资源 → 404
- 前端 JS / CSS 加载失败、白屏

### 修复

**新建 [`scripts/copy-standalone-assets.mjs`](../../scripts/copy-standalone-assets.mjs)**：

- 在 `.next/standalone/` 下递归查找含 `server.js` 的目录作为"运行时根"
  （单包项目 = standalone 根；monorepo / 有父级 lockfile 时 = 子目录如 `agent-01/`）—— 不
  硬编码 `agent-01`，将来父级 lockfile 删除后无需改脚本。
- 用 Node `fs.cpSync({recursive:true, force:true})` 跨平台拷贝；`public` 不存在时跳过；
  完成后打印运行根 + 启动命令。
- 二次收口：拷贝前先清理旧目标目录，避免旧 chunk 残留；`.next/static` 作为必需资源，
  若缺失则脚本直接失败，阻止坏 standalone 产物进入部署流程。

**`package.json` 加 `build:standalone` 脚本**：

```json
"build:standalone": "next build && node scripts/copy-standalone-assets.mjs"
```

部署链路只需把 `next build` 替换为 `npm run build:standalone`，其余不动。

### 验收（方案 §二.4 标准）

| 检查项 | 结果 |
|---|---|
| `.next/standalone/agent-01/.next/static` 存在 | ✅ 含 `chunks/` `media/` 等 |
| `.next/standalone/agent-01/public` 存在 | ✅ 含 `favicon.ico` 等 |
| `ci:typecheck` / `ci:lint` / `ci:build` | ✅ 通过 |
| `node .next/standalone/agent-01/server.js` + `/_next/static/...` 200 | ✅ 本地 standalone 验证通过 |
| `node .next/standalone/agent-01/server.js` + `/favicon.ico` 200 | ✅ 本地 standalone 验证通过 |

补验命令：

```txt
npm run build:standalone
node .next\standalone\agent-01\server.js
GET /_next/static/chunks/02ad55c0df2a1468.js -> 200
GET /favicon.ico -> 200
```

部署机仍需上线前复验同一组检查，避免 CI 产物与部署打包流程不一致。

---

## P1 · 知识库智能体真实 chat 端到端（已 done）

方案 §三人工验收结论：

- **`glm-4-air`** 端到端命中 KB、严格遵守（"wsl 代号 plamguns" / "西红柿会吃人" 都按 KB 答）
- **`glm-4-flash`** 即便叠 inline 注入 + 禁词列表 + 角色扮演，仍用世界常识反驳 KB —— **不可用于知识库智能体**

详见 [5.19up 变更记录 · 第 4 轮收口 + 「知识库智能体最低可用模型 = glm-4-air」](../5.19up/变更记录-20260519.md#L640) 段。

[5.19up 上线清单 C2](../5.19up/上线清单-20260520.md) 已标「已拍板：glm-4-air」。

### 5.20up 代码收口

- `app/admin/agent-builder/[id]/page.tsx`
  - 智谱模型预设改为 `glm-4-air（知识库推荐）`，`glm-4-flash` 改为“不建议知识库”。
  - 已绑定知识库且当前有效模型为 `glm-4-flash*` 时，前端发布按钮拦截并提示换 `glm-4-air` 或更高模型。
  - org_admin 无知识库可选时，不再提示去「知识库管理」，改为提示联系平台管理员。
- `app/api/admin/agent-drafts/[id]/publish/route.ts`
  - 后端兜底：绑定知识库的智谱智能体不能用 `glm-4-flash*` 发布，避免绕过前端。
- `app/admin/knowledge-bases/page.tsx`
  - 接回 `AdminLayout`，知识库管理进入后台统一导航框架。
- `app/admin/knowledge-bases/[id]/page.tsx`
  - 接回 `AdminLayout`，详情/上传/重建/删除页面进入后台统一导航框架。

---

## P1 · 知识库 prompt 单点化 + 失败 / 空命中口径收紧

### 起因

5.19up 三轮收口把硬规则 prompt 内联到 `chat/route.ts` 与 `test-chat/route.ts`，两处长串重复、
易漂移；同时存在两个隐患：① 检索"空命中"时不注入任何规则、② 检索"失败"时只 `console.warn`
后降级 —— 两种情况下，绑了知识库的智能体仍会用常识答题，**用户无法区分这是"知识库里的答案"
还是"模型自己的常识"**，与 5.19up 验收锁的「以知识库为唯一事实依据」口径相违。

### 改动

**新建 [`lib/kb/prompt.ts`](../../lib/kb/prompt.ts)** —— 知识库注入提示词的单点出口，导出 2 个函数：

- `buildKbStrictAnswerPrompt(chunks)` —— 正常检索路径
  - **chunks 非空**：原 5.19up 三轮的硬规则文案（不可用常识反驳资料、禁词列表等），并在
    ③ ④ 处加严：「资料未覆盖时，必须直接答『知识库中没有找到相关资料，无法回答该问题。』，
    不要另起段补充常识」。
  - **chunks 为空（新增分支）**：返回新的硬规则要求模型直接答固定句「知识库中没有找到相关
    资料，无法回答该问题。」，禁止任何常识补答。
- `buildKbUnavailablePrompt()` —— 检索异常降级路径（新增），要求模型直接答固定句「知识库
  暂时不可用，无法基于知识库回答该问题。」，禁止常识补答。

**`app/api/agents/[id]/chat/route.ts`** ⚠ 最高风险文件：
- 内联硬规则字符串 → `kbInjectText = buildKbStrictAnswerPrompt(chunks)`。
- catch 分支由「只 `console.warn`，不注入」→ 仍 `console.warn` 留可观测性 + 注入
  `buildKbUnavailablePrompt()`，让模型显式告知"知识库不可用"，**不再用常识冒充知识库答案**。
- 其余流程不变；非知识库智能体（无 `kbIds`）零影响。

**`app/api/admin/agent-drafts/[id]/test-chat/route.ts`**：同 chat route 同步同一套（保持
chat / test-chat 同口径，5.19up 已确立的原则）。

### 语义变化（重要 · 写给小B 复审）

| 场景 | 5.19up 行为 | 5.20up 行为 |
|---|---|---|
| 知识库 chunks 命中 ≥ 1 | 注入硬规则 + 资料块 | 同（文案在 ③ ④ 处略严，禁止补充常识段） |
| 知识库 chunks 命中 = 0 | **不注入**，模型按"无知识库"用常识答 | **注入空命中规则**，模型答"没找到相关资料" |
| 知识库检索抛错（embedding / RPC） | `console.warn` + **不注入**，模型按"无知识库"用常识答 | `console.warn` + **注入不可用规则**，模型答"知识库暂时不可用" |

**带来的副作用**：
- 知识库智能体绑了库以后，命中差或检索失败时不再"伪装成普通智能体"用常识答，会明确说"没找到 / 不可用"。
  这是有意为之 —— 否则用户分不清答案来自哪里，违反验收期口径。
- 不影响 **未绑知识库** 的智能体（`kbIds.length === 0` 时 `kbInjectText` 仍为空，行为完全不变）。
- 不影响 **非 openai / 智谱平台**（条件检索本就跳过这些平台）。

### 验证

- `ci:typecheck` / `ci:lint` / `ci:build` 通过。
- 端到端复测项（待人工跑）：
  - 绑库 + 命中：按 KB 严格答（保持 5.19up 验收行为）。
  - 绑库 + 空命中：答"知识库中没有找到相关资料，无法回答该问题。"（新增行为，须人工验证）。
  - 绑库 + 检索失败（断 embedding provider 或 v38/v39 未跑）：答"知识库暂时不可用，无法基于
    知识库回答该问题。"（新增行为，须人工验证）。
  - 未绑库 / 扣子 / Dify / 工作流跑批：行为不变。

---

## P1 · AdminLayout 加 fullBleed / hideFooter prop（配套知识库接回布局）

知识库列表 / 详情页本次接回 `AdminLayout` 统一导航，但页面本身是沉浸式宽屏 + 自带页脚，
直接套默认 `AdminLayout` 会出现「右侧内容被 `max-w-[1600px]` 居中 + 底部多一条白色 footer」
的体感倒退。

**`components/layout/admin-layout.tsx`**：`AdminLayout` 新增两个可选 prop：

- `fullBleed?: boolean`（默认 `false`）—— 跳过默认的 `p-5 sm:p-7 max-w-[1600px] mx-auto`，
  让页面自己铺满右侧内容区。
- `hideFooter?: boolean`（默认 `false`）—— 隐藏默认 footer，避免沉浸式页面底部露出白框。

知识库列表 / 详情页传 `fullBleed hideFooter` 后维持原全宽体验；其它管理页未传 prop，默认值
等同于原行为，**零影响**。

---

## P1 · 网络代理误伤外部服务（用户决定本期先不管）

方案 §四原拟同步修部署机代理规则；用户决定 **5.20up 不处理，本机开发期已有应对方式**
（手动给代理软件加 `bigmodel.cn` / `supabase.co` / `github.com` 的直连/代理路由）。
部署机上线前另行核验，本节落空不出代码。

---

## P2 · 可延后项（本期不动）

方案 §五（CI 自动化测试）、§六（Next 构建警告 / middleware 迁移）均为 P2，本期不动；
上线后单独排期。

---

## 涉及文件

```
新建：scripts/copy-standalone-assets.mjs
修改：package.json                                       （加 build:standalone）
修改：app/admin/agent-builder/[id]/page.tsx              （知识库推荐模型 / glm-4-flash 拦截）
修改：app/api/admin/agent-drafts/[id]/publish/route.ts   （后端兜底拒绝 glm-4-flash + KB）
修改：app/admin/knowledge-bases/page.tsx                 （接回 AdminLayout · fullBleed）
修改：app/admin/knowledge-bases/[id]/page.tsx            （接回 AdminLayout · fullBleed）
新建：lib/kb/prompt.ts                                   （KB 注入提示词单点出口 · 空命中 / 不可用分支）
修改：app/api/agents/[id]/chat/route.ts                  （改用 prompt.ts · catch 分支注入"不可用"）
修改：app/api/admin/agent-drafts/[id]/test-chat/route.ts （同 chat 同步同一套）
修改：components/layout/admin-layout.tsx                 （加 fullBleed / hideFooter prop）
新建：upgrade/5.20up/变更记录-20260520.md（本文件）
```

---

## P0 · API 管理「默认模型」字段加回（5.27up Fix 误删的回归修复）

### 起因（用户实测踩坑）

`5.19up/变更记录-20260519.md` 的"API 管理列表 · 去掉「默认模型」列"原本只想砍**列表的展示列**，
保留编辑弹窗 / DB 字段 / 运行时兜底。但 5.27up 紧接着的 Fix 把 **编辑弹窗的输入框**、
**切厂商联动填模型**、**`formFromPreset` 用 `preset.defaultModel` 预填新建表单** 三处一并砍了
（注释里写"admin 不在 provider 层设默认，agent 必须自己选"）。

后果：admin 在 API 管理新建一条非 OpenAI 兼容厂商（千问 / DeepSeek / 豆包 / 文心 …）的
provider 时，`default_model` 入库 `""`，但 [test/route.ts:83](../../app/api/admin/model-providers/[id]/test/route.ts#L83)
`const model = (provider.default_params?.model as string) || provider.default_model || "";`
会被 adapter 层兜底链「`agent.model_params.model → provider.default_model → openai 平台
gpt-4o-mini`」（CLAUDE.md 已记录）顶到 `gpt-4o-mini`，被千问 DashScope 直接返：

```
404 The model `gpt-4o-mini` does not exist or you do not have access to it.
```

5/27 用户连续测试火山方舟→DeepSeek 官方→通义千问 三家时，前两家因为是 DeepSeek/Volcengine
官方协议 + 错误位于"key 自身"层（401 / 402），没触发到这个兜底；切到千问那一刻就炸了。

### 改动

均在 [`app/admin/model-providers/page.tsx`](../../app/admin/model-providers/page.tsx)。
**DB schema / 后端 POST·PATCH / `test` 路由 / `streamChat` 兜底链 — 全部不动**，只动前端表单：

1. **`formFromPreset`**（line ~99）：`default_model: ""` → `default_model: preset.defaultModel`。
   新建场景从厂商预设取默认（如选「通义千问」→ 自动填 `qwen-plus`；选「DeepSeek」→
   `deepseek-chat`；选「智谱 GLM」→ `glm-4-air`；选「OpenAI」→ `gpt-4o-mini` 等）。
2. **厂商下拉 onChange**（line ~524）：与 endpoint / default_params 同口径加 `default_model`
   联动 —— 字段为空或仍是旧预设的原值就覆盖；admin 已手填过就保留不动。
3. **编辑弹窗"默认模型"输入框**（line ~588）：恢复 `model` / `embedding` category 下显示
   （`agent` category 不显示，与原 5.27up Fix 注释里"智能体平台模型在 bot 上"口径一致）；
   placeholder 为当前厂商预设的 `defaultModel`；下方加 helper 文案明示「不填则运行时
   兜底 `gpt-4o-mini`，非 openai 平台会被拒」。
4. **`openEdit` 加历史脏数据修复**（line ~174）：DB `default_model` 为空时（5.27up Fix
   期间新建的 provider 入库就是空字符串），用 `inferPresetFromExisting` 反查到的厂商预设
   默认模型补上 —— 否则编辑场景里 form state 仍是空，admin 不主动填字段保存还是空，
   测试又会兜底 `gpt-4o-mini`。已有 DB 值则保持不动。
5. **文件顶部注释**：清掉 5.27up Fix「default_model 已移除」的旧注释，加 5.20up 本次回归
   修复说明。

### 不动的

- **列表的「默认模型」列**：5.19up 用户拍板"列表展示多余"，保持不展示（口径不回退）。
- DB 表 `model_providers.default_model` 字段：本来就还在，本次不改。
- 后端校验 / 写入逻辑（[`route.ts`](../../app/api/admin/model-providers/route.ts) /
  [`[id]/route.ts`](../../app/api/admin/model-providers/[id]/route.ts)）：原本就接受
  `default_model`，本次不改。
- 智能体侧覆盖：agent.model_params.model 优先级仍最高，本次不影响搭建器 / 智能体管理的
  模型选择逻辑（用户口径"agent 必须自己选模型"在 agent 层仍有效，本次只是补 provider
  层的兜底默认值）。

### 验证

- `ci:typecheck` / `ci:lint` 通过。
- 用户实测复测项（须人工跑）：
  - 新建千问 provider：默认模型自动填 `qwen-plus` → 保存 → 测试 → 200 + "连接成功"。
  - 编辑现有 provider：默认模型字段显示 DB 原值，可手改。
  - 切厂商：默认模型按新预设自动填（若 admin 已手填过则保留不动）。
  - 选「智能体 API」tab：默认模型输入框不显示（与旧行为一致）。

### 涉及文件

```
修改：app/admin/model-providers/page.tsx   恢复默认模型输入框 + 切厂商联动 + 新建预填
```

---

## P1 · 厂商预设升级到 2026/05 最新一批模型

### 起因

5.27up 厂商预设里的 `defaultModel` 当时（5.27up 写入时）就已老化：`gpt-4o-mini` 已不是
旗舰、`qwen-plus` 被 qwen3.x 系列取代、`deepseek-chat` 将于 2026-07-24 弃用、`glm-4-air`
不再是智谱当前主力。用户实测千问 provider 时撞上 `gpt-4o-mini` 兜底 404（见上一节），
顺势统一刷新 [`lib/model-providers/presets.ts`](../../lib/model-providers/presets.ts) 的
LLM_PRESETS 全部 7 家（豆包 / 火山方舟用接入点 ID 不动）到 2026/05 当前最新。

### 厂商更新表（用 WebSearch 核实过 model ID 的 4 家明确改 default，其它 3 家因 model ID
命名格式无法 100% 确认，保留旧默认 + hint 提示用户在控制台拿到准确 ID 后手填覆盖）

| 厂商 | 新 default | 把握 | 备注 |
|---|---|---|---|
| OpenAI 官方 | `gpt-5.5` | ✅ 高 | 2026/04/23 GA；recommendedModels 加 5.5 / 5.5-pro / 5 / 4o |
| 智谱 GLM | `glm-5` | ✅ 高 | 2026/02 GA；⚠ **hint 明确写：知识库智能体仍按 5.20up 验收锁定 `glm-4-air` 起步，glm-5 未端到端复测** |
| 通义千问 | `qwen3.6-plus` | ✅ 高 | 用户点名；2026/04/02 GA · 1M 上下文。recommendedModels 加 qwen3.7-max（2026/05 旗舰）|
| DeepSeek | `deepseek-v4-flash` | ✅ 高 | 2026/04/24 GA；hint 提醒 `deepseek-chat` / `reasoner` 旧别名于 2026-07-24 弃用 |
| Kimi / Moonshot | `moonshot-v1-128k`（沿用，由 -8k 升 128k）| ⚠ 中 | platform.moonshot.cn OpenAI 兼容 ID 命名未明确；hint 注 2026/04 GA K2.6，请在控制台确认 ID 手填 |
| 文心 ERNIE | `ernie-4.5-turbo-8k`（猜测命名）| ⚠ 中 | 千帆 V2 OpenAI 兼容接口 ID 未明确；hint 注 ERNIE 5.0 / 4.5 Turbo 已 GA，请在千帆控制台确认 ID 手填 |
| 混元 Hunyuan | `hunyuan-turbo`（沿用）| ⚠ 中 | 腾讯云 OpenAI 兼容 ID 未明确；hint 注 Hunyuan TurboS（2026/02 GA）请在腾讯云控制台确认 ID 手填 |
| 豆包 / 火山方舟 | `""` 不动 | — | 接入点 ID 模式，必须 admin 手填 `ep-xxx` |

### 不动的

- **endpoint**：全部 8 家保持不变（厂商 base URL 没有变迁）。
- **`recommendedModels` 字段已有结构**（最近某次改动加进去的下拉数据源）：本次只更新内容、
  不改字段定义。
- **DB / 后端 / adapter**：完全不动。
- **5.20up 知识库智能体 `glm-4-air` 验收锁定口径**：仍有效，仅在 provider 默认值层面用更新的
  `glm-5`，agent 侧的选择和拦截逻辑（`agent-builder` glm-4-flash 拦截）不动。

### 风险与跟进

- ⚠ Kimi / 文心 / 混元 三家的"新版本默认 ID"我没在官方文档里核实到准确 OpenAI 兼容
  `model` 字符串 —— 仅在 hint 里注明位置和方法。**新建这三家 provider 测试若 404，是预设
  里我写的 ID 与厂商实际命名不符，admin 自行覆盖即可，不是代码 bug。**
- 智谱 `glm-5` 替换 `glm-4-air` 作 default 是 5.20up 后的口径软变动 —— **不影响已建的知识库
  智能体**（agent 侧绑模型独立保存）。新建知识库智能体仍应在搭建器里手选 `glm-4-air` 起步，
  且 hint 已明示。

### 验证

- `ci:typecheck` / `ci:lint` 通过。
- 端到端复测项：
  - 新建千问 provider → default 自动填 `qwen3.6-plus` → 测试通过。
  - 新建 DeepSeek provider → default `deepseek-v4-flash` → 测试通过。
  - 新建智谱 provider → default `glm-5` → 测试通过 + hint 显示 `glm-4-air` 知识库锁定说明。
  - 新建 OpenAI provider → default `gpt-5.5` → 测试通过（前提：账号有 GPT-5.5 访问权 + 余额）。

### 涉及文件

```
修改：lib/model-providers/presets.ts   7 家厂商 default + label + hint + recommendedModels 更新到 2026/05 最新
```
