### 6.4up 方案 · 知识库综合优化（召回 + 记忆 + 监控）

日期：2026-06-03
作者：小A
状态：R0 待用户勾选方案后出 R1
位置：`upgrade/6.4up/`
依据：用户 2026-06-03 dev 实测「办公环境管理智能体（anthropic / claude-opus-4-8）+ 已绑 KB」发现 ——

1. RAG 召回：「我第一次轮值是啥时候」召回 0 chunks（threshold 0.5 卡掉），但「轮值表」原文关键词能命中。
   用户原话：「客户会用自己的方式去问，不是 KB 原文措辞」
2. 记忆：多轮对话后 KB 内容"忘了"。用户原话：「这个智能体的记忆也没做好，跟智能体聊着聊着就把知识忘记了」
3. 监控：「你有没有能够监控记忆的工具」—— 当前无任何 prompt 监控工具

---

## A · 当前实现速记

| 维度 | 现状 | 文件 |
|---|---|---|
| RAG 检索 | top-K=5、cosine threshold=0.5 | [lib/kb/config.ts:23-26](../../lib/kb/config.ts#L23-L26) |
| query embedding | 仅用当前 user message，**不带历史** | [lib/kb/retrieve.ts:37](../../lib/kb/retrieve.ts#L37) |
| chunks 注入 | 每条 user message **前缀**拼 `kbInjectText`（不入 history）| [app/api/agents/[id]/chat/route.ts:478-480](../../app/api/agents/[id]/chat/route.ts#L478-L480) |
| 多轮累积 | ❌ 无。每轮独立检索，旧 chunks 立即丢失 | — |
| history 上限 | 20 轮 / 40 条 | [lib/config.ts:41](../../lib/config.ts#L41) |
| Prompt 监控 | ❌ 无。`logs` 只记 token / model / duration | — |

---

## B · 三大子主题 · 全方案候选

### 子主题 1 · RAG 单轮召回率

> 痛点：「我第一次轮值」语义距离 > 0.5 被丢掉

#### ☐ 1-A · 降 threshold 0.5 → 0.35

- 改 [lib/kb/config.ts:26](../../lib/kb/config.ts#L26) 一行
- 实施 10 秒；用户复测 1 分钟
- ✅ 立即见效
- ❌ 召回噪声片段，弱模型 / 紧凑模型可能被噪声误导 hallucinate
- ❌ 没解决「代词 / 模糊指代」类 query
- 工程量：**~10 min**

#### ☐ 1-B · query 改写（LLM rewrite）

- 在 `retrieveKbChunks` 前加一步：用便宜模型（如 gpt-4o-mini / haiku-4-5）把 user query 改写成 3 个语义不同的检索 query；每个 embed + 检索，合并去重 top-K
- 例：「我第一次轮值是啥时候」→ ["轮值排班表", "第一轮值班时间", "我的轮值日期"]
- ✅ 召回精度高
- ❌ 每轮 +1 LLM 调用，+ ~500ms 延迟、+ ~200 token 成本
- ❌ 改写模型对 Claude / 智谱 / OpenAI 平台都要适配（或硬编一个 rewriter provider）
- 工程量：**~3-4h**（含 prompt 设计 + 改写模型选型 + dev 测）

#### ☐ 1-C · history-aware query（历史拼接检索）

- query 改成 「最近 N 轮 user message + 当前问题」拼接后再 embed
- 例：第 3 轮 user 问「刚才那个职责具体是啥」→ query = 「轮值职责\n第一轮排班\n刚才那个职责具体是啥」
- ✅ 解决「代词 / 模糊指代」型问法
- ✅ 0 状态管理、不调额外 LLM
- ❌ 跨话题对话时 query 焦点模糊
- ❌ embedding 输入变长（多 ~300 token，影响不大）
- 工程量：**~1h**

#### ☐ 1-D · 不改（教 admin 用「明确措辞」）

- 写一段 admin 知识库使用须知，告诉 admin / 客户「问问题时用 KB 里的关键词原文」
- ✅ 0 改动
- ❌ 用户原话否决：「客户会用自己的方式去问」

#### ☐ 1-E · 组合 · A + C（降 threshold + 历史拼接）

- 1-A 提整体召回率，1-C 解决代词问题，两个都便宜
- 工程量：**~1.5h**

#### ☐ 1-F · 组合 · A + B + C（全套）

- 召回率天花板，但成本最高
- 工程量：**~5h**

---

### 子主题 2 · KB 多轮累积记忆

> 痛点：第 N 轮丢失第 1 轮 KB 注入的内容

#### ☐ 2-A · 简单累积 chunks 池

- 加 `sessionRetrievedChunks` 数组到 conversation 状态，每轮 KB chunks 加入并去重，全量注入到当前 user message
- ✅ 实现最简单（~30 行）
- ❌ 第 10 轮可能 50 chunks ≈ 25000 token，Opus OK 但 Haiku 紧
- ❌ 旧话题无关 chunks 残留干扰
- ❌ 跨话题时 prompt 混杂
- 工程量：**~1-2h**

#### ☐ 2-B · 滚动窗口累积（LRU N=8）

- 加 `KB_CONTEXT_MAX_CHUNKS = 8`，按 LRU 淘汰；多轮内固定 ~8×500=4000 token 上限
- ✅ token 可控
- ✅ 老话题 chunks 自然淘汰
- ❌ 跳话题瞬间会"短暂混杂"
- ❌ 需要 conversation 级状态管理（DB 或前端 ref 传回）
- 工程量：**~3h**（含状态持久化方案）

#### ☐ 2-C · history-aware 检索（同 1-C，复用）

- 不真累积 chunks，而是让检索能召回"上次提过的相关内容"
- ✅ 0 新状态
- ❌ 仍依赖 embedding 距离，「具体的"5 月 12 日"日期」这种事实型记忆仍会丢
- 工程量：与 1-C 合并，**0 额外**

#### ☐ 2-D · system role 持续注入 KB 摘要

- 每轮把"近 N 轮命中过的 chunks 摘要"放进 system role
- ✅ system 多轮持续可见，符合 LLM 角色设计
- ❌ Claude 中转吞 system → 对当前 anthropic provider 没用（5.30.1 R4 已知）
- ❌ 智谱 / OpenAI 可用，但增加 system 长度
- 工程量：**~2h**

#### ☐ 2-E · LLM 自检索（tool use）

- LLM 每轮自己决定要不要 call `search_kb(query)` tool；调用后 chunks 流回 LLM
- ✅ 最智能，按需检索
- ❌ 需要 tool_use 支持；anthropic 已有 / OpenAI 已有 / 其它平台不一定
- ❌ 实现复杂度最高（需要 tool-loop 处理）
- 工程量：**~1 天**

#### ☐ 2-F · 摘要 + 滚动（hybrid）

- 每 N 轮把之前的 KB 上下文压缩成摘要 + 保留最近 K 个 raw chunks
- ✅ 平衡 token + 长期记忆
- ❌ 复杂度高、需调摘要质量
- 工程量：**~1 天**

#### ☐ 2-G · 不做（保持每轮独立）

- 0 改动
- 用户已表达不满，应该做

---

### 子主题 3 · Prompt / 记忆 监控工具

> 痛点：用户看不到 LLM 实际收到了什么，没法 debug

#### ☐ 3-A · console.log 临时插桩（最快诊断）

- 在 [chat/route.ts:481](../../app/api/agents/[id]/chat/route.ts#L481) `messages` 数组组装后加一次性 console.log
- 输出：每个 role 的 content 前 200 字符 / KB chunks 数 / 总 token 估算
- 只在 dev 环境用，commit 时带条件 `if (process.env.NODE_ENV === 'development')`
- ✅ 10 分钟搞定
- ❌ 只看 dev console、用户端 admin 看不到
- 工程量：**~30 min**

#### ☐ 3-B · admin「会话调试」面板（产品化中级）

- admin 进 `/admin/conversations` 列表 → 任一对话 → 看每轮的 system / history / KB 前缀 / 完整 user message
- 需要新表 `chat_prompts (conversation_id, message_id, system_prompt, history_json, kb_chunks_json, user_message, created_at)`
- 每轮 chat 完成时写一条
- admin UI 加一个查看页
- ✅ 永久审计
- ❌ 隐私 / 安全：prompt 含 user 原话和 KB 资料，需要权限控制（只 super_admin 可见 ？）
- ❌ 存储成本（每轮 ~10KB，1 万对话 ≈ 100MB）
- 工程量：**~1 天**

#### ☐ 3-C · 完整对话审计 + 检索可视化（高级）

- 3-B + 显示每轮 KB 命中的 chunks（含相似度分数 + 文档名 + 章节）+ token 使用按 role 拆分
- 还能"重放"：选某轮的 prompt，重新发给当前模型对比效果
- ✅ 终极 debug 工具
- ❌ 工程量大，UI 也复杂
- 工程量：**~3 天**

#### ☐ 3-D · 不做监控

- 0 改动
- 上面 1+2 完成后召回 / 记忆 已经好了，admin 看不见也能用
- 但若 1+2 实施后还有问题，没监控就没法定位

---

## C · 工程量总览

| 组合 | 工程量 | 适合场景 |
|---|---|---|
| 1-A only | 0.5h | 临时救场，先看效果 |
| 1-A + 1-C（推荐套餐 1）| 2h | 单轮召回急救 |
| 1-A + 1-C + 2-B + 3-A | ~6-7h | **平衡推荐** · 召回 + 记忆 + dev 监控 |
| 1-F + 2-E + 3-C | ~5 天 | 全套终极版 |

---

## D · 实施顺序（你选完方案后我按这个走）

```
Phase 0 · 写代码（按你勾选的方案）
Phase 1 · typecheck + lint
Phase 2 · dev 实测（你帮跑）
Phase 3 · 写变更记录 + commit
```

如果勾的方案涉及 DB 改动（2-B / 3-B / 3-C），加：
```
Phase -1 · 新建 migration_vXX.sql 让你在 supabase Dashboard 跑
```

---

## E · 文件改动清单（按方案选择动态）

下表是不同方案选项各自要改的文件，最后我会按你勾选汇总。

| 方案 | 改动文件 |
|---|---|
| 1-A · 降 threshold | `lib/kb/config.ts:26` |
| 1-B · query 改写 | `lib/kb/retrieve.ts`（加 rewrite 步骤）+ 新 `lib/kb/rewrite.ts` |
| 1-C · history 拼接 | `lib/kb/retrieve.ts` 签名加 historyMessages 参数 + `app/api/agents/[id]/chat/route.ts` 传 history + `app/api/admin/agent-drafts/[id]/test-chat/route.ts` 同改 |
| 2-A · 简单累积 | `chat/route.ts` 加 conversation 级 chunks 累积（DB 或 cache）|
| 2-B · 滚动 LRU | 2-A + 新表 `kb_context_state` 或前端 ref 传回 |
| 2-D · system 持续 | `chat/route.ts` KB 注入位置改 system role |
| 2-E · tool use | adapter 接 tool_use loop、`chat/route.ts` 大改 |
| 2-F · 摘要+滚动 | 2-B + summarize 步骤 + 摘要 LLM 调用 |
| 3-A · console.log | `chat/route.ts` 加条件日志 |
| 3-B · 调试面板 | 新表 + `app/api/admin/chat-prompts/route.ts` + `app/admin/conversations/page.tsx` |
| 3-C · 完整审计 | 3-B + 检索可视化 + 重放功能 |

---

## F · 验收（按勾选方案动态）

每个方案的验收点都在 B 节里。R1 出来时合并成完整验收清单。

---

## G · 分支策略

默认沿用 `feature/6.2up-platform-input`（同 6.3up 套路）。如果你勾的方案涉及 DB 改动（2-B / 3-B / 3-C），考虑独立分支 `feature/6.4up-kb-optimize` 便于上线时与 6.2/6.3 分开。

---

## H · 下一步

1. **你勾选 B 节里要的方案**（每个子主题选 1 个或多个组合）
2. 我按勾选出 R1：合并影响范围 + 完整代码片段 + 验收清单
3. R1 通过 → Phase 0-3 实施

---

## 我的推荐（仅供参考，你拍板覆盖）

```
🔴 子主题 1 · RAG 召回：1-E（1-A + 1-C）
🟠 子主题 2 · 记忆累积：2-B（滚动 LRU 8）
🟡 子主题 3 · 监控：3-A（console.log 临时插桩）

总计 ~6h · 一次实施搞定召回 + 记忆 + 基本 dev 调试能力
```

理由：
- 1-A + 1-C 互补（threshold 降 + 历史拼接），不上 LLM rewrite 省成本和延迟
- 2-B 比 2-A 智能（旧 chunks 自然淘汰），比 2-E 简单（不依赖 tool_use）
- 3-A 先做 dev 用，3-B/C 等 6.5up 再产品化
